# 货币战争智能狸开发交接（2026-08-01）

## 1. 当前结论

用户已经否决 0.2.752–0.2.755 作为日常使用版本，并要求回滚到 0.2.751。
原始 0.2.751 发布包仍在，已验证程序集版本、ZIP 结构和 SHA-256。

当前源码不是 0.2.751 源码。由于没有 Git 历史或对应源码快照，只能把它视为
“0.2.751 之后的开发现场”。下一位开发者必须审计，而不是继续叠加功能。

## 2. 0.2.751 用户回滚基线

发布包（交接包内）：

`release-baseline/CurrencyWarsSmartRaccoon-0.2.751-win-x64-portable.zip`

- ProductVersion：0.2.751
- FileVersion：0.2.751.0
- ZIP 条目：1697
- SHA-256：`C9F62D904744477305679EE417F2C360C53E895EE2CD5807E85CD2E248497675`

0.2.751 已包含：

- 红黑色生命值耗尽终局页识别；
- 失败末节点封存；
- 报告生成及“结束后删除截图”设置；
- 详细历史入口和实时历史信息展示；
- 旧式失败页与生命耗尽页的视觉区分。

它并不代表没有已知问题；只是用户当前要求回到这个行为基线。

## 3. 0.2.752–0.2.755 经手变更

### 0.2.752

- 删除“识别当前画面”“进入开局页”两个无效运行按钮；
- 修正启动耗时统计边界和大型筛选列表虚拟化；
- 修复敌人概览异步结果过早缓存；
- 调整未知角色身份下累计伤害更新；
- 正式历史仅展示完成节点。

### 0.2.753

- 详细历史关闭按钮事件和界面调整；
- 过滤 `Battle`、`battle_generic`、`reward_shop` 等伪节点；
- 完美规则：血量变化 +2；血量未知且行动严格大于 10；
- 缺失伤害单位时不再直接猜测数量级。

### 0.2.754

- 实时截图间隔从 200ms 改为 100ms；
- 战斗到备战切换强制作为关键边界；
- 保留快速分类未命中的紧邻前序帧；
- 关键 OCR 队列满时保存失败截图和 manifest；
- 详细历史增加 Esc 关闭并抑制透明辅助窗抢点击。

### 0.2.755

- 单实例进程和重复启动唤醒；
- 启动配置并行读取；
- 启动说明改为非模态；
- 大型开局词条编辑区改为按需加载；
- 开始记录/刷开局先刷新反馈，再执行同步准备；
- 悬浮窗创建调整到较低调度优先级。

用户只给出总体结论“新版本太多 bug”，没有逐项确认是哪一项导致问题。
因此以上修改全部是“待审计候选”，不能整体认定为稳定修复。

## 4. 识别需求和数据规则

仍然有效的用户要求：

1. 血量优先读取战斗结束黄色成功页；不足时再从下一节点备战主界面降级读取。
2. 黄色页读取当前血量、获得金币总览、前三角色及伤害。
3. 节点总伤害和前三角色伤害是独立字段，不得覆盖。
4. 关键页面采用有限多帧，保留最高置信度有效结果。
5. 空值或低置信度不得覆盖已有成功结果。
6. 记录节点、页面来源、原始 OCR、标准化值、置信度和降级原因；失败区域保存截图。
7. 不得通过无限重试或单一硬编码坐标掩盖 DPI、截图时机或状态切换问题。

## 5. 断点恢复现状

主要文件：

- `src/CurrencyWarsAssistant.Advisor/RunCheckpointContracts.cs`
- `src/CurrencyWarsAssistant.Advisor/LocalRunStore.cs`
- `src/CurrencyWarsAssistant.Tasks/Phase2LiveCollectionService.cs`
- `src/CurrencyWarsAssistant.App/SituationAnalysisViewModel.cs`

行为：

- 开始记录即创建断点；可靠状态变化或约每 10 秒保存；
- 原子替换、WriteThrough 和 `.bak` 备份；
- 主断点损坏时读取备份，必要时从已保存产物合成残缺断点；
- 游戏窗口/截图失败最多等待两分钟，然后保存为 Paused；
- 当前节点相同或更晚时续录，更早时建立新对局，身份冲突或节点未知时询问用户；
- 窗口进程重启后句柄改变，当前收集器不会自动重新绑定新句柄。

专项测试最后为 17/17，但没有执行真实断电或实机窗口崩溃验证。

## 6. 当前源码最后一次离线验证

- 定向界面测试：18/18；
- 全量：549/549；
- 最终源码版本标记：0.2.755；
- 0.2.755 自包含包曾在本机约 2.07 秒写出 StartupCompleted；
- 重复启动进程约 0.48 秒退出；
- 这些结果不能推翻用户的实机失败反馈。

## 7. 下一阶段推荐方式

1. 不要把当前源码直接发布给用户。
2. 不要仅将 `Directory.Build.props` 改成 0.2.751。
3. 先向用户收集最先希望保留的一个功能或一个可复现问题。
4. 从当前源码提取该单项修改，加入专门测试。
5. 用全新版本号创建候选包，和 0.2.751 并存。
6. 用户确认后再处理下一项。
7. 每次保留上一个可运行 ZIP 和 SHA-256。

## 8. 构建和测试

项目需要 .NET 8 SDK。交接包不携带本机 `.tools` SDK。

```powershell
dotnet test .\CurrencyWarsAssistant.sln -c Release

dotnet publish `
  .\src\CurrencyWarsAssistant.App\CurrencyWarsAssistant.App.csproj `
  -c Release -r win-x64 --self-contained true `
  -o .\artifacts\CurrencyWarsSmartRaccoon-<version>-win-x64-portable
```

正式交付前必须写清：离线测试范围、失败/跳过数量、未验证项、包路径和 SHA-256。

## 9. 隐私和安全

- 不要打包 `%LOCALAPPDATA%\CurrencyWarsSmartRaccoon`。
- 不要打包用户反馈截图中的 UID，除非用户明确要求且理解风险。
- 不删除用户运行记录；破坏性操作前解析并确认精确路径。
- 当前工作区没有可信 Git 历史，修改前自行备份相关文件。

