# 交接补充（20260830 晚）—「另一 AI 路线移植 + 复审修复」轮

> 本文件是 HANDOFF_FOR_NEW_AI_20260830.md 的**追加章节**，覆盖 20260830 晚的最新一轮。
> 上一份交接文档的背景/机制/铁律全部继续有效，本文件只记录增量。

## 本轮做了什么（按另一 AI 的工作路子移植 + 补齐 + 排坑）

另一 AI 在独立副本上做了 7 轮工作后其副本被清理，留下的是一份详细的总结报告。
本棒 AI 按该报告把**全部验证过的修复**移植进主工作区，并补齐它承认未完成的三项，同时修掉它在过程中引入/暴露的新问题。全部改动已过子代理对抗式复审（阻断 3+应修 6 逐条处置）+ 全量门禁（1017/1034，12 败=既有环境敏感清单，零新增回归）。

### 移植落地的改动（对应另一 AI 轮次）
| 来源 | 落地内容 |
|---|---|
| 二轮提速 | 导航轮询 300→50ms/稳定帧 2→1；WaitForPageAsync 350→50（**保留双帧危险闸**——它在此踩过单帧击穿撤退确认的坑）；grail 局数上限 0=不限（用户拍板可能上百局）；晶矿同步 900→200；开书重试 1200→300 |
| 三轮 #17 | 购买规划器加 `currentDeployedPlacements`（实时满员判定，1-2 不再按进奖励关前旧快照重复购买），1-1/1-2 两调用点透传 `currentFormationPlacements` |
| 三轮 | Fast 启用（`FastReroll=Fast`，参数链一行接通） |
| 五轮 | PrepareFastAsync 动画竞态容错：单帧识别为 0 → 3×450ms 重试 → 稳定版 ReadStableBenchAsync 回退（针对"合格开局被误判阵容不满足弃掉"） |
| 六轮 | **缩放修复**：3 处快速路径拖拽坐标改 `MapReferencePoint`（2560×1440 下原版潜伏的 (231,305) 偏移缺陷——Fast 启用后必踩，已逐点替换并构建验证） |

### 本棒 AI 按自己审计补齐的（另一 AI 未做）
| 项 | 落地 |
|---|---|
| B1 双泵 | opening 弹框泵在 opening 返回后 Cancel+await——否则与 1-3 循环双应答同一弹框，WishesResponded 双计数 |
| B3 快照污染 | 只有 preparation_* 帧参与运营组装（商店/战斗/结算残帧会产出空阵容快照误拒奇迹代偿） |
| D1 诅咒拆分 | 回路过载=**买经验**价格+1（N17 门槛动态 8→10）；行为限制=**刷新**价格+1。两条诅咒分开判定（数据文档 127 行明示） |
| D2 聘用书匹配 | "四费聘用书"与"五费聘用书"编辑距离=1，模糊匹配显式排除四费（精确子串排除法） |
| D7 金币清零 | 奇迹代偿选中即把金币缓存清 0（代价=扣 88 血+全部金币，选择动作的游戏事实） |
| 策略偏好 | 补 333「都是这家伙的错！」（官方名，注意不是"全是"） |

## 复审发现并已修复的问题（本轮复审抓出来的，全部已修）

1. **R4 活锁**：弃局后 `continue` 留在 1-3 循环内，主页帧被门禁拒绝组装 → 180 秒后再次弃局 → 永动弃局。修：弃局后 `return`，外层 for 从主页开新一轮。
2. **N14 商店页活锁**：商店 Pass 没买到时不关店 → 页面停留 reward_shop → N14 路由绕过 180s 兜底永久循环。修：Pass 无论买没买到**必关店**，页面回 preparation 交决策器。
3. **局数上限失效**：MainWindow 显式 `MaxRounds=20` 覆盖了 0=不限。已移除。
4. **弃局异常外泄**：coordinator 两处弃局调用无保护，异常会击穿整跑（违背"绝不整体终止"）。已加 try/catch + 事件发布。
5. **退出按钮无页面门禁**：弃局先分类当前页，已回主页则零输入返回。
6. **5 费判定口径统一**：执行器卖人过滤与组装器统一为"纯 [5]"（银狼=3 费，用户拍板）。
7. **录屏双构造**：MainWindow 构造了两个 recorder，第二个覆盖第一个但 finally 收尾的是第一个 → 录像错配。已去重。

## 移植后仍未完成（给下一棒的清单，按优先级）

1. **上场非命杯角色卖出**：数据通路已铺好（快照 `DeployedNonGrailCharacters` 含卡牌区域；卖法=场上卡牌直接拖到既有 `SellTargetPoints` 出售区，坐标现成）。缺：执行器 `GrailSellDeployedCharacterAsync` 动作 + RunPreparationLoop 的卖出路由。**这是下一个最大价值点**。
2. **购买上限"场上 3 人即停"的实况计数**：#17 修复后 1-2 已用实时 placements，但 N13 仪式（统一上场命杯成员）的部署不走规划器——若需把 N13 上场者也计入"3 人"，需把 N13 的部署结果并入 `currentFormationPlacements`。
3. **应修 6（复审原文）**：opening 期弹框泵依赖采集会话帧，而会话在 opening 成功后才启动 → opening 期泵是"盲"的。若实机证实 1-1/1-2 会弹框且卡死，需改泵为 trialSelection 轻量直接探测（不依赖管线帧）。当前依赖 20s 监测+弃局兜底兜住。
4. **12 项既有环境敏感测试失败**（PENDING_USER_FIXES.md），与本次改动无关。

## 实机联调重点（下一棒第一件事）

1. 重刷导航速度（本轮提速后应显著改善）
2. 命中 067/019 → 1-1/1-2 商店买命杯（同名只买一次应生效）→ N2 星徽 → N13 仪式
3. 1-3 策略未命中时**不再弃局**（选最左推进）
4. 祈愿弹框自动应答（注意 opening 期弹框泵是盲的——见上）
5. 停止（按钮+热键）立即停手

## 验证命令

```
dotnet build CurrencyWarsAssistant.sln -c Release
dotnet test tests/CurrencyWarsAssistant.Tests -c Release --no-build --filter "FullyQualifiedName~Tests.Grail"   # 79/79
dotnet test CurrencyWarsAssistant.sln -c Release --no-build   # 全量：1017/1034，12 败=既有环境敏感清单
```
