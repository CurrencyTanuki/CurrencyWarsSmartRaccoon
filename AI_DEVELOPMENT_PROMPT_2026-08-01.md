# 可复制给无上下文 AI 的开发提示

你正在接手 .NET 8 WPF 项目“货币战争智能狸”。先完整阅读
`HANDOFF_START_HERE.md` 和 `HANDOFF_2026-08-01.md`，再查看源码。

关键版本事实：用户已经否决 0.2.752–0.2.755，并要求使用原始 0.2.751。
交接包中的 `release-baseline/CurrencyWarsSmartRaccoon-0.2.751-win-x64-portable.zip`
是用户当前回滚基线；但源码已经包含到 0.2.755 的修改，且没有 Git 历史和
0.2.751 源码快照。不得把当前源码称为“已经回滚”，不得只改版本号伪装回滚，
也不得一次性重新发布所有后续修改。

工作方式：

1. 先确认用户本轮只要求修复或新增哪一个最小功能。
2. 审计当前源码中与该功能有关的 0.2.752–0.2.755 修改，其他修改不要顺手扩大。
3. 保持既有数据结构和识别栈；不要新建平行应用或重复 OCR 管线。
4. 识别数据必须保留来源、原始 OCR、标准化值、置信度和不确定原因。
5. 黄色成功页优先读取血量、获得金币总览、前三伤害；下一备战页只作为有界降级。
6. 节点总伤害与前三角色伤害、金币余额与结算奖励必须是独立字段。
7. 低置信度、空值和低数量级候选不得覆盖已成功读取的高置信度数据。
8. 不通过无限重试、全局降低阈值或单一分辨率坐标掩盖问题。
9. 先写能复现问题的定向测试，再做最小修复；之后运行相关测试和一次全量回归。
10. 当前机器没有游戏，绝不能声称实机验证完成。
11. 输出新的独立候选 ZIP，不覆盖 0.2.751；提供准确路径、版本和 SHA-256。
12. 如果测试集不足以确定字段含义或坐标，只询问最小必要信息，不猜测。

优先保护用户数据。断点记录默认位于
`%LOCALAPPDATA%\CurrencyWarsSmartRaccoon\runs`，不得擅自删除或迁移。

完成后报告：根因、修改文件、识别/降级逻辑、测试结果、未验证项、候选包路径、
回退方式。测试通过不是用户实机认可的替代品。

