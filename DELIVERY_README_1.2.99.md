# 货币战争智能狸 v1.2.99 交付说明

《崩坏：星穹铁道》货币战争玩法自动化辅助。截图识别+模拟输入，不读内存、不改游戏文件。

## 它能做什么（自动刷取循环）

1. **自动刷开局**：反复开局，只接受两种投资环境——**067 英雄登场 / 019 命运圣杯邀请**（其余环境含圣杯七变体一律判不合格，立即弃局重开，~40 秒/轮）。
2. **命中局自动运营**：开晶矿 → 商店购买命杯成员并部署上场（学者凑 2 补位/1-3 必卖）→ 出战打 1-1/1-2 → 选投资策略 → 1-3 圣杯循环刷商店买成员 → 金币耗尽自动弃局 → 回到 1 重开。整夜无人值守循环。
3. **自愈能力**：游戏断线弹窗自动识别并停机告警；识别流偶发冻结由看门狗在 2 秒内自动重启恢复；结算卡死走盲点直通回主界面；连续 5 次弃局失败自动停机等人工。

## 怎么用

| 操作 | 方法 |
|---|---|
| 启动软件 | 计划任务 `CWSmartRaccoonCmdTest`（开始菜单搜"任务计划"，右键运行）；弹出测试台窗口 |
| 开自动刷取 | 在 exe 同目录建 `指令测试-command.txt`，内容一行 `DECIDE`（保存后约 1 秒被消费） |
| 停止刷取（引擎停止，识别保留） | 同上发指令：内容一行 `DECIDE 停止` |
| 停止识别会话 | 发指令 `STOP`（注意：这停的是识别采集，不是决策引擎） |
| 完全退出软件 | 建 `指令测试-exit.txt`（内容随意），软件空闲时自动退出 |
| 急停 | 建 `指令测试-abort.txt`（内容随意），当前在途指令立即中断 |
| 查状态 | 发 `STATUS`，回执在 `指令测试-result.txt`（含流诊断行） |

游戏须由用户自行启动并登录到货币战争主界面；软件绝不自动启动游戏。

## 日志（排障看这里）

- 事件日志：`%LOCALAPPDATA%\CurrencyWarsSmartRaccoon\logs\test-session-*.jsonl`
- 黑匣子（每条指令/耗时）：同目录 `grail-flight-*.jsonl`
- 关键事件速查：`命中环境=`（进局）/`已放弃不合格开局`（弃局）/`I7StarBadgeLocated`+`GrailBadgeAssemblyAttempt`（星徽装配）/`I7StarBadgeProbeFailed`（星徽探测异常）

## 已知限制（如实告知）

1. **识别流周期性冻结**（约 15~25 分钟一次）：根因未修，看门狗 2 秒自动恢复，每次拖慢半分钟以内，不影响循环连续性。
2. **晶矿球检测有误检**（检测数偏高、个别球漏开）：拍板接受现状（有开矿动作即可），不修。
3. **装备明细识别漏读**（只修了星徽一件事）：星徽装配经模板定位器+账本兜底，其余装备明细不影响决策。
4. 全量测试在本机有 15 个环境性失败（OCR/性能噪音，单跑全过，与代码无关）。

## 版本要点

- 1.2.90~98：上场台账（防换下命杯）、学者规则（仅 1-1 凑 2、1-3 必卖）、弃局点法重写+盲点直通、识别流诊断、统计页卡死热修、**星徽装配链修复**（I7 改走模板定位器实拍）。
- 1.2.99：最终交付剥离——KEY/CLICK/SCREENSHOT 三条 AI 诊断指令已移除。
