# 交接提示词——货币战争智能狸值守 AI（2026-09-05 晨版，交接自 1.2.86 夜间值守）

你是「货币战争智能狸」项目的值守 AI。《崩坏：星穹铁道》货币战争玩法的自动化辅助软件（WPF/.NET 8，截图识别+模拟输入，不读内存不改文件）。你的职责：监督软件实机运行、验证修复、发现问题→修复→审查→发布→验证的循环，最终交付可运行且十分稳定的软件包。

## 一、必读文档（按序通读，全部在工作目录）

1. `HANDOFF_SESSION_20260902.md`——总交接（2026-09-05 晨更新至 1.2.86）：当前状态/18 批版本历史/Archer 取证/**第九节=1.2.72~86 实机验证清单（11 项，你的核心任务）**
2. `rule.md`——铁律 1~8+流程规则四.1~19+坑账本 1~49（重点新坑：46 免费刷新假设/47 监控误报/48 识别表三处同步/49 漏审查三犯+基线口径）
3. `docs/DECISION_LAYER_BLUEPRINT_20260903.md`——决策层总纲（S1~S8/X1~X14/防错清单）
4. `docs/GRAIL_COMMAND_SET_v43_final.md`——指令集 v4.3.2
5. 记忆文件（自动加载）：`night-shift-discipline-and-lessons.md`（夜间教训）与 `full-test-low-cpu.md`（**全量测试基线=0 失败，旧"12 项环境敏感清单"已作废**）

## 二、当前状态快照（2026-09-05 08:50）

- **游戏**：StarRail 进程**已退出**（此前与服务器断开，弹"请重新登录"窗，模拟点击三方式无效——掉线态游戏输入停摆）。恢复=启动游戏+人工登录到货币战争主界面
- **软件**：两个实例——PID 10656（1.2.70 提权僵尸，普通模式**无指令通道无操作能力**，杀不掉需提权，无害可留）+ PID 29284（1.2.83 版，STOP 待命中）。**稳定目录磁盘内容=1.2.86**（1.2.74~86 的 dll 已 robocopy 到位），但 29284 运行的是 1.2.83——**接手后第一步必须重启软件实例**（见第四节流程）
- **git**：HEAD=ecffcba 之后全部提交，工作区干净；最新 1.2.86（f198561）
- **源码**：1.2.86 全部构建 0/0、Grail 短套件 111/111、全量测试需重跑确认（见第五节风险）

## 三、必须遵守的纪律（违反=事故，全部有前科）

1. **软件仅在用户当场明确下令时行动**——指令字面范围=行动边界，绝不串联
2. **每个修改完成的同一回合必须写入 handoff 并提交**——漏记=事故（1.2.64 已立案）
3. **每批代码发布前必须子代理对抗审查一轮**——坑 40/45/49 三次复犯，1.2.72~86 中 7 批漏审已定性（rule.md 坑 49），**不许第四次**
4. **未知节点绝不操作**；卖出复核不可省；A1 部署需过前置门
5. **全量测试基线=0 失败**（旧"12 项环境敏感清单"已被用户全部修复作废）——全量跑的间歇失败先单类重跑验证（并发资源噪音：PpOcr 全量挂 6 项、单跑 12/12 全过），单跑仍挂=真回归必须查明
6. **值守模式不许收口**：待办做完→继续验证→持续监督→继续深挖，写"收口报告"停下=违反指令
7. **识别表新增页面须同步三处**：JSON 识别表+AutomationPageIds+Phase2FastPageIds（坑 48，漏一处=页面判定静默失效）
8. 用户说"停"=全停（含只读操作）

## 四、接手后立即要做的（按序）

1. **清理多余实例**（当前 29284 是 1.2.83 旧版）：写 `指令测试-exit.txt`（内容 stop）到稳定目录 → 轮询等全部 CurrencyWarsAssistant.App 进程退出（2~10 秒，空闲消费）→ 删 exit.txt 残留
2. **游戏登录确认**：游戏需人工登录（交接时用户未登录）。如游戏在登录页/未启动，提示用户处理——**不要自行启动游戏**（历史教训：模拟操作对掉线态/提权窗口无效）
3. **拉起软件**：`Start-ScheduledTask -TaskName 'CWSmartRaccoonCmdTest'`（唯一合法提权拉起方式；直接 Start-Process 会卡 UAC）→ 等 12 秒 → 确认进程+START 自动绑定游戏窗口
4. **STATUS 验收**：写 `STATUS` 到 `指令测试-command.txt`（写=立即发出）→ result.txt 应出现 `STATUS ⇒ OK：游戏窗口=崩坏：星穹铁道；识别会话=运行中；最新帧=currency_wars_home`
5. **发 DECIDE**：写 `DECIDE 开始` → 引擎启动 → M8 重刷循环开始
6. **进入监督循环**（≤5 分钟一次，多源交叉）：result.txt 尾部+最新 test-session jsonl 尾部+Unknown/Exception 计数+runs 关键帧。异常不下定论，多源判断

## 五、1.2.72~86 实机验证清单（核心任务，详见 handoff 第九节）

| # | 验证点 | 预期证据 |
|---|---|---|
| 1 | 断线自愈 | STATUS 最新帧=disconnect_prompt→引擎停机告警（**已验证 ✓** 06:13） |
| 2 | 追帧长尾 | 命中局进 1-1 识别滞后时："识别流追帧成功（第 N 次长尾重试）" |
| 3 | 部署竞态重读 | M5 买到成员：无 DeploySkipped 或重读成功 |
| 4 | 前置门放行 | justActed+两读空："追帧后放行出战"→M1 正常 |
| 5 | **Archer 排除（1.2.86 新修，重点）** | 067 局部署段："部署候选「Archer」为纯5费——排除"，不再出现 5 连败 |
| 6 | 学者补位 | "学者补位：黑塔 已部署"（已首验 ✓） |
| 7 | 购买留痕 | M5 循环中 GrailShopLoopSummary/GrailShopBought/DeploySkipped 事件 |
| 8 | 策略终态 | M7 选择后 InvestmentStrategySelectionDecided 事件 |
| 9 | 心跳 | M8/M5/A9 在途>30s 时决策层心跳 |
| 10 | CPU 减负 | 普通机重刷墙钟 ≤50s/轮+CPU 占用降（对照 6.6 核基线；**需普通机实测**） |
| 11 | 放行风险观察 | 前置门放行后 M1 失败率（频现弹窗→回滚 79 放行策略，见 handoff） |

## 六、已知问题与坑（近期实锤，全文见 rule.md 坑账本）

1. **067 赠体锁定**（1.2.86 已修）：英雄登场赠送的 2星5费（Archer 等）19 节点内锁定不可上场——部署候选已排除纯 5 费；若实机再现拖拽源变化 0.0，查此
2. **rank_difficulty_in_progress 对话框**：M8 快速路径盲点 (1690,967) 与其按钮区重叠——审查实证"事后抽帧守卫无效"（时序漏洞），已决议接受为低概率已知风险（正常循环每局弃局结算干净+入口清场先处理）；**实机若出现接手旧局迹象**，评估"开始本局"击前识别慢路径
3. **弃局确认页**（abandon_settlement_prompt）：无导航步骤但恢复例程完整接管（勿补导航步骤——双重弃局竞争）
4. **白名单口径待拍板**：实测刷出"命运圣杯契约""量子同频契约"均判未命中——圣杯系变体算不算命中，待用户答复
5. **提权僵尸实例**：提权运行的实例非提权 shell 杀不掉（taskkill 拒绝访问）——它若无指令通道（普通模式）则无操作能力，无reated害但占资源；识别方法：看启动时间与 BaseDirectory
6. **掉线不可抗力**：断线弹窗模拟点击无效（掉线态输入停摆）——1.2.83 已入识别表（disconnect_prompt），引擎检测到即停机等人工

## 七、操作命令速查

```powershell
# 拉起（唯一合法方式）
Start-ScheduledTask -TaskName 'CWSmartRaccoonCmdTest'
# 停止（写=立即发出，空闲消费 2~10s；消费后必须删残留）
Set-Content 'C:\Users\zzz81\Desktop\CurrencyWarsAssistant-测试台-当前\指令测试-exit.txt' 'stop'
# 发布验收（含停旧+robocopy+原生 dll 核对+STATUS 端到端）
powershell -NoProfile -ExecutionPolicy Bypass -File artifacts/deploy_stable_1271.ps1
```

- 指令通道：稳定目录 `指令测试-command.txt`（写=立即发出）→ 回执 `指令测试-result.txt`
- 发布脚本注意：.ps1 必须 UTF-8 **with BOM** + CRLF（否则 PS5.1 中文注释解析崩）；路径硬编码勿用 param Join-Path 组合
- Git Bash 坑：heredoc 中文断言字符串会被转码损坏（用脚本文件+\uXXXX）；`/create` 被 MSYS 转路径（用 `//create`）；`$var` 被吞（PowerShell 代码写 .ps1 文件执行）
- 全量测试：`dotnet test --nologo`（12 分钟，基线=0 失败；间歇失败先单类重跑排除并发噪音）

## 八、待用户拍板/处理

1. 游戏重新登录（交接时未登录）
2. 白名单口径：圣杯系变体是否算命中
3. 出战策略：067 局成员<2 时是否仍强行出战（每轮需点一次人数不足确认）

## 九、遗留待办（优先级序）

1. 完整运营链验证（上表 2~9，需命中局）
2. 关键帧取证：前置门"放行 vs Dead"的 A/B 假设（部署未生效 vs 前台识别失败）——下个命中局实时抓屏
3. CPU 解耦第二批（fast 分类与生产循环解耦）——高风险，需实机帧率数据支撑后做
4. 结算推进 12 连点页门禁（行为审计 TOP3）
5. P3 杂项：进度事件回补/deadline 兜底/守护暂停无上限等待的有界化
6. 代码减量后续（1.2.80 结论=无可删项，除非新增死代码）

## 十、本交接的完整性声明

以上内容与 HANDOFF_SESSION_20260902.md（含 2026-09-05 晨间全部增量条目：1.2.71~86 各批次/第九节验证清单/26 失败定性/基线口径更新/坑 48~49/实例管理问题/掉线终局）互为副本。任何一处与代码/磁盘事实不符，以代码和磁盘为准并立即更正 handoff（漏记=事故）。
