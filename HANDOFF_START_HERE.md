# 货币战争智能狸：零上下文交接入口

交接日期：2026-08-01（Asia/Shanghai）

如果你是第一次接触本项目，请严格按以下顺序阅读：

1. 本文件；
2. `HANDOFF_2026-08-01.md`；
3. `AI_DEVELOPMENT_PROMPT_2026-08-01.md`；
4. `docs/UPDATE_NOTES_0.2.751.md` 至 `docs/UPDATE_NOTES_0.2.755.md`；
5. 再检查源码和测试，不要先重构。

## 1. 最重要的版本事实

- 用户当前明确要求回滚并使用 **0.2.751**。
- `release-baseline/CurrencyWarsSmartRaccoon-0.2.751-win-x64-portable.zip`
  是原始 0.2.751 自包含发布包，SHA-256：
  `C9F62D904744477305679EE417F2C360C53E895EE2CD5807E85CD2E248497675`。
- 本交接包里的源码却已经包含 0.2.752–0.2.755 阶段的修改，
  `Directory.Build.props` 仍标记为 0.2.755。
- 项目没有可用的 Git 历史，也没有精确的 0.2.751 源码快照。
- 因此：**不得声称当前源码已经回滚到 0.2.751，也不得只修改版本号伪装回滚。**
- 后续开发必须以 0.2.751 原始程序作为用户认可的运行基线，以当前源码作为
  “包含候选修复但已被用户判定 bug 过多的待审计源码”。

## 2. 项目入口

- 解决方案：`CurrencyWarsAssistant.sln`
- WPF 启动项目：
  `src/CurrencyWarsAssistant.App/CurrencyWarsAssistant.App.csproj`
- 主要实时识别：`src/CurrencyWarsAssistant.Tasks`
- 数据/状态契约：`src/CurrencyWarsAssistant.Advisor`
- 图像与窗口捕获：`src/CurrencyWarsAssistant.Vision`
- 测试：`tests/CurrencyWarsAssistant.Tests`
- 游戏版本数据：`data/4.4`

这是 .NET 8 WPF 项目。禁止读取或修改游戏内存、注入游戏进程、修改游戏文件。
程序只使用截图、OCR、模板匹配、像素证据和 Windows 输入。

## 3. 当前验证边界

- 当前源码最后一次全量离线测试：549/549 通过，0 失败、0 跳过。
- 断点专项测试：17/17 通过。
- 这些结果只证明测试覆盖的行为，不证明 0.2.752–0.2.755 已得到用户认可。
- 当前开发机没有安装游戏；禁止声称完成实机对局验证。
- 用户已经明确反馈新版本 bug 太多并要求回滚，因此用户反馈优先于测试通过数量。

## 4. 后续 AI 的第一任务

不要立即发布 0.2.755。先完成版本审计：

1. 保持 0.2.751 原始包不变并记录哈希；
2. 阅读 `HANDOFF_2026-08-01.md` 的变更清单；
3. 将 0.2.752–0.2.755 的修改按功能拆分；
4. 对用户仍需要的修复逐项重新验证，不能把所有后续修改一次性重新合入；
5. 每次只交付一个小范围候选版本，并提供可回退包；
6. 任何无法由截图、日志或测试确定的字段必须保持 Unknown，不得猜测。

## 5. 打包排除项

本交接包不包含：`.tools`、`bin`、`obj`、临时日志、用户对局目录、
用户设置、旧发布包、0.2.752–0.2.755 可执行包以及测试临时输出。
只额外包含 0.2.751 原始回滚包作为行为基线。

