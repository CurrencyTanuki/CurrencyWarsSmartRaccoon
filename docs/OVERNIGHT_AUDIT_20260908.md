# 通宵实测逐局审计报告（2026-09-08 深夜 → 09-09 早 9:00）

> 值守 AI 通宵任务：1.2.120(死锁修复版)发布后整夜挂机刷局，逐局详细审计。
> 用户审计维度（原文口径）：①软件逻辑问题/缺口；②**操作质量**——冗余步骤、操作过慢、
> 鼠标坐标偏移导致首点未命中（第二次才点上=成功但有冗余点击）、坐标偏移触发降级、
> 冗余点击/行为。用户已自行找到一部分，值守必须独立抓出来。
> 方法：每局"三问"+rule 8.2 八不变量推演+操作质量逐帧核对；禁止批量脚本糊弄。
> 证据处理：分析完确认无异常的证据文件/帧目录直接删除（用户令），异常证据保留并标注。

## 〇、今晚版本与起点

- 修复：1f61037 引入的 DI 解析死锁（详见 handoff 第〇节）→ 逐接口显式工厂。
- 版本：**1.2.119+a2c6556**（watcher 02:53 自动部署,DEPLOY-OK+VERIFY-PASS）
- 起点：02:53:47 DECIDE 自动启动（autodecide 一次消费成功），游戏已在玩法主界面。

## 一、逐局审计

### 第 1 局（02:59:36 Matched → 03:13:02 弃局，约 13.5 分钟）

- **时间窗**：02:53:47 启动 → 4 次重刷弃局（各 ~30-34 秒）→ 02:59:36 Matched → 1-1 开矿/商店 → 1-2/1-3 推进 → 03:08:50 卖镜流复核失败反证即停 → 03:13:02 快照恢复预算耗尽弃局。
- **结果**：未完成（1-3 中段）。弃局标签="快照持续不可得（恢复预算耗尽）"——诚实 ✓。
- **三问**：①该做吗——M8 刷局/进局/卖冗余均符合决策树 ✓；②做对了吗——商店购买/晶矿/部署序列正常,卖出复核失败后立即停手（坑38）✓；③失败处置对吗——快照恢复 3 轮→带标签弃局（簇E 终态机）✓,A9 链 preparation_generic→normal_hud 干净 ✓。
- **不变量**：无违反（无卖出后金不增、无部署到已占槽、无主界面 Esc）。
- **操作质量**：识别分析流停滞 ×2（87s/75s+）为弃局根因——见 P3-1；I10 在非备战页每 5 秒失败重试=日志噪音（候选优化:节流）。
- **证据处置**：run 目录与 jsonl 保留至早上复核后清理。
- **视频维度审计（用户要求第 3+ 数据源,首战）**：从死亡实例自动收尾的 298MB DECIDE 录像抽帧目视验证——①03:08:47 帧（镜流卖出失败时刻）：**商店开着(阮·梅/艾丝妲/灵砂/乱破/Saber 五槽)、后台区域 6 格全空**——引擎试图从"备战席 1 号位"卖镜流时备战席根本没有镜流（快照备战席模型错误/过期,正处识别流停滞窗口）→ 反证即停**拦下一次基于错误盘面的卖出** ✓；②03:12:55 帧：清场后空棋盘=弃局链中段 ✓。异常帧保留于 sandbox-frames/recording-audit/。次生发现：录像文件名内嵌时间戳"20260907_185347"是过期 temp 段名(1.2.113 已知风险族,命名引用了陈旧 temp 数据)。

### 第 2 局（03:23:01 Matched → 03:38 弃局，约 15 分钟）

- **时间窗**：M8 第 11 轮命中（重刷 9.5 分钟,节奏 ~51 秒/次）→ 1-1 → 03:28:03 凑息静默放行（无可卖,正确）→ 03:28:28 M1 出战 → 03:28:41 M7 选中"二极管"（单人模式+二极管=继续打,四.13a 合规 ✓）→ 1-3 商店循环金=0 收摊 → 03:35-03:38 I10"非备战页帧"循环 → 快照恢复 3/3 → 03:38 弃局。
- **结果**：未完成（1-3,金=0）。弃局标签=快照持续不可得——诚实 ✓。
- **三问**：①②合规（凑息/策略/收摊全部正确）；③失败处置——快照恢复 3 轮后弃局,但**根因=识别流第 3 次分析冻结（03:35 起 ≥3 分钟）**。
- **重大发现（P1 级运行缺陷,已紧急修补 e714d08）**：三次停滞全部为"**心跳续传但完整分析冻结**"形态——复活判活用的是心跳时间戳（永远新鲜）而非最后完整分析时间戳,导致 ForceStreamRevive/引擎 staleness 全部判"流健康"拒绝重启,追帧等到地老天荒。修补=补"无祈愿/盛会弹框门禁 ∧ 最后完整分析>45 秒→判冻结"分支（弹框门禁防 1.2.118 幻影重启复辟——LastUpdateAt 设计注释读透后确认边界）。复核子代理审查中,过审即重新部署。
- **证据处置**：保留。

### 04:11 修复版重部署后的新会话（1.2.119+1baf888 构建,内容含 e714d08+4fc13cd 全部修补）

> 注:版本串指向 1baf888 是构建时序瑕疵(P1 修补在工作树中先构建后提交 4fc13cd)——二进制内容已经 162 套件验证含全部修补。流程改进待办:**提交后再构建**,保证版本串=内容。

- **G1（04:12→04:30,败北）**：命中进局→1-1→1-2 凑息（金=6<10,保护线正确跳过云璃/吉尔伽美什,无可卖静默放行——rule 49 ✓）→ 1-2 战斗败北 → normal_hud → M8。无违规。
- **G2（04:30→04:47,1-3 R3 山穷水尽）**：M1 出战 → 1-3 → M7 选二极管（单人合规）→ 商店扫买 → 金=0 收摊 → 卖丹恒·腾荒后复核"Bench 2 号位不符"反证即停（卖后卡位前移导致校验槽位错位——**操作质量候修:复核应按卡名而非固定槽位**）→ 对账恢复（羁绊=2 金=3）→ S1A 卖到保留线 → **R3 正确判死**（金<刷新价∧可卖=0,前置全部核实）→ 带标签弃局。全链路 M8→1-1→1-2→1-3→R3 首次完整走通 ✓。
- **修补生效验证**：04:32:20 引擎"识别流冻结→请求重启→START 成功"——分析年龄判据路径打通(修补前此路死)。停滞恢复从"永不/87s+"改善为"45-50 秒内主动复活"。

## 〇-b、预热审计（09-07 22:22 会话=1.2.117+72da03f，52 分钟 6 局，用户新维度的首次应用）

数据源：test-session-20260907-222223.jsonl(2729 事件)+input-diagnostics.jsonl(窗口内 35 拖拽)+指令测试-result.txt(782 条终态)。提取工具：artifacts/audit_extract_20260908.py（输出在 artifacts/audit-prep/）。

### 操作质量发现（用户维度）

- **P3-热1 弃局链首次 Esc 系统性冗余**：44 次弃局中 40 次出现"Esc 尝试 1/2 → 4.4 秒后判未成功 → 尝试 2/2 立即成功"的固定模式（RecoveryKeyRetry=40）。即 91% 的弃局浪费 1 次按键+4.4 秒等待 ≈ 累计 3.2 分钟/52 分钟。疑似首 Esc 的成功判定窗口/判据过严，而非游戏不响应（第二次总是"成功"）。
- **P3-热2 拖拽 holdMs=0 违背坑44 口径**：35 次拖拽中 32 次 holdMs=0、durationMs=650；仅 3 次(300/450ms)。坑44 要求部署/出售拖拽 250ms 按住。两次失败重试（见热3）均发生在 holdMs=0 拖拽。需核对 M5 买人自动上场拖拽是否漏配 250ms。
- **P3-热3 失败重试=最终成功但冗余**（用户点名的形态）：22:37:23 部署远坂凛失败→22:37:27 重试（4 秒）；22:50:51 出售银狼(左侧出售区)失败→22:50:56 重试(改右侧出售区,卡片位置已移动 28px)。52 分钟内 2 次,与 PreparationDeployRetry=2 吻合。
- **P3-热4 商店首帧 OCR 不稳定**：ShopRecognitionPartial=33 次,大量"帧1 乱码/帧2 稳定"——两帧校验兜住了识别,但部分槽位被跳过购买(自动购买属可选增强)。无错误购买,属识别带宽损耗。
- **正面确认**：弃局链整体 ~24 秒/次干净完成(结算页识别→连点→回主界面),无 1.2.118 时代"Esc 打在无效页面"的卡死(本窗口未遇到投资环境模态/祈愿弹框在屏的弃局场景);刷新点击(61 次)节奏 ~3 秒/次正常;无任何坐标越界(此前疑似的 (2447,1240)/(2483,479) 经核实=2560×1440 全屏客户区内的合法右侧出售区/物品栏坐标)。

### 遗留疑点（待今晚实测复核）

- 弃局首 Esc 判定为何总失败——读 CurrencyWarsRejectedOpeningRecovery 的 KeyAttempt 判据(成功判定是否要求"页面已变化"而首 Esc 后页面切换需要 >4.4 秒动画)。
- holdMs 配置流向:M5 买人上场拖拽与 A2/A3 出售拖拽的 hold 参数来源是否一致。

## 二、Bug 与缺陷汇总（持续更新）

### P1（功能性错误）
（无）

### P2（规则/口径违反）
（无）

### P3（操作质量/性能/冗余）
1. **识别分析流反复停滞（顶层问题）**：修补(e714d08+4fc13cd)后复活路径已打通（04:32:20/05:11:34 两次引擎自动请求重启成功），但**管线在商店重 OCR 阶段"重启后再挂"仍发生**（05:27 一次快照不可得弃局漏网）；追帧长尾重试次数偏高（单局最多第 8 次）。量化：05:xx 时段 9 次追帧成功、1 次漏网弃局。晨间深挖：2560×1440 帧的商店页分析耗时画像+挂起堆栈（需要提权外诊断点）。
2. **"M5 收摊后 I10 拒读循环"模式（3+ 次实锤：G1/G2/G5 全部死于此后遗症）——晨间代码复查后假设修正**：CloseShopAsync（RewardStageAutomation.Shop.cs:81+）有完整三段防护（已关→OK/未知页→有界等待/非商店页→不点返回），面板残留假设降级。修正后的统一根因=**分析管线 Mode B 真冻结**：面板关了、页面已是 preparation_generic，但识别流最新分析停在几分钟前（I10 读流=拒读）→ 追帧等不到 → 预算耗尽弃局。与 P3-1 同根。**晨间取证方案：对比 I10 拒读时刻的 (a)流最新分析时间戳 (b)CloseShopAsync 的 ReadStablePageAsync 读数（后者是新抓帧,前者是流缓存）——若 (a) 旧 (b) 新=分析管线停,坐实 Mode B。候选缓解：I10 拒读 N 次后改走"单帧实拍快照"降级路径（管线外旁路,风险=绕过统一识别流架构,需设计评审）。**
3. **卖出后复核槽位错位（2 次：丹恒·腾荒/风堇）**：卖 N 号位后复核查 N-1 号位（卡位前移模型与游戏实际不符）→ 反证即停（安全 ✓）但本可不停止。候修：复核按卡名与"原槽为空"双条件,槽位按 shift-left 模型修正。
4. （预热审计 4 条见〇-b：弃局首 Esc 冗余 40/44、holdMs=0 32/35、重试成功×2、商店首帧 OCR 不稳。）
5. **jsonl 0 字节之谜（P2,不影响运行）**：04:11 实例的 test-session jsonl 创建后 29 分钟 0 字节（Publish 未落盘），但 input-diagnostics/决策叙述/run 目录正常。不影响审计（决策叙述最全），晨间用转储定位 UiTaskEventSink 写入链。

## 三、计数器（08:28 晨间终盘，result.txt 累计含昨日晚间会话）

| 指标 | 值 | 备注 |
|---|---|---|
| 命中(067 英雄登场/019 圣杯邀请) | 31 | 含 067×2 次拿到赠体 |
| R3 山穷水尽弃局 | 8 | **全部前置核实**(金<刷新价∧可卖=0),含 05:54"确认仍有可卖→不判 R3→再卖"的正确两段式 |
| 快照不可得弃局 | 11 | 修补前 2 局必死;修补后漏网 3 局(G? 05:27/06:39/07:13)——管线"重启后再挂"形态 |
| 冻结自动复活 | 42 | 引擎"检测冻结→请求重启"全部执行(修补前此路死) |
| DECIDE 启动 | 2 (02:53 watcher 自动恢复 / 04:11 修复版重部署,autodecide 均 ✓) |
| 完成局数 | 0 | **诚实底线:没有一局打到收工**——全部以 R3/败北/快照不可得结束;识别流停滞是最大瓶颈 |
| 卖出复核失败 | 3 (镜流/丹恒·腾荒/风堇,反证即停 ✓) |

## 四、晨间深挖清单（按优先级）

1. **识别分析管线 Mode B 真冻结取证**（P3-1/P3-2 同根,顶层):I10 拒读时刻对比"流最新分析时间戳 vs 单帧实拍读数";2560×1440 商店页分析耗时画像;提权外诊断点(启动看门狗标记文件思路可复用)。
2. **I10 拒读降级旁路设计评审**:"拒读 N 次后改单帧实拍快照"——绕统一识别流架构,需方案评审后实施。
3. **jsonl 0 字节之谜**:UiTaskEventSink 写入链转储(04:11 实例 29 分钟 0 写)。
4. **卖出复核按卡名+shift-left 槽位模型修正**(2 次误停)。
5. **弃局首 Esc 冗余 40/44**(判据窗口)、**holdMs=0**(32/35,坑44 口径)、**商店首帧 OCR 不稳**(两帧校验兜底)。
6. **版本串时序瑕疵**:提交后再构建(今晚两次构建 stamp=父提交)。
