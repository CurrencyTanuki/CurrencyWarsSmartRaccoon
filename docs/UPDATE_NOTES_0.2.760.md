# 货币战争智能狸 0.2.760 验收说明

> 本次交付三大能力：①开局信息进对局记录；②已完成对局后台数据可查（含阵容/装备/背包/投资环境/策略）；③UI 一致性改进。
> 涉及玩法机制部分均已先查询项目内攻略数据与识别区域文档确认（见下"玩法依据"）。

## 一、开局信息完整记录（敌人阵营 / 负面词条 / 投资环境）

- **链路打通**：开局小助手识别到的开局数据（`OpeningSnapshot`：3 个投资环境候选、敌人阵营、负面词条）此前只用于刷开局判断，从不落盘。
  现通过 `OpeningRerollLoopProgress.Snapshot` → `UnifiedRunLifecycleService` → `LiveCollectionStartOptions.InitialIdentityEvidence`
  → checkpoint `IdentityEvidence` → `completed-run.v1.json`，完整保存。
- **玩法依据**：敌人概览页固定读取 3 敌人阵营 + 4 负面词条（SCREEN_FLOW_1920x1080.md:129-193）；投资环境开局 3 候选选后整局生效（同文档 :219-290）。
- **说明**：自动刷开局（达标后进 1-1 记录）路径已完整记录开局信息；手动录制从备战开始，无开局数据时标记"未记录"。

## 二、已完成对局后台数据可查（新增"历史对局"窗口）

- **入口**：节点历史悬浮窗 →"详细历史"窗口 → 右上角新增 **"历史对局"** 按钮。
- **内容**：左侧列出所有已完成对局（运行 ID、完成时间、节点数、结束节点）；右侧显示：
  - **开局信息**：投资环境、敌人阵营、负面词条（名称化显示）；
  - **每节点完整后台数据**：节点概况、**历史阵容与角色装备**（每角色 3 装备槽）、**装备栏与局内资源**（右侧物品栏）、构筑羁绊敌情、最终战斗、伤害明细、识别诊断。
- **数据来源**：直接读取磁盘 `completed-run.v1.json`（此前没有任何代码读取该文件——这是"界面有字段但数据为空"的根因）。
- **阵容/背包/装备未记录的排查结论**：识别层（备战帧 board/bench/装备槽/物品栏）与契约层（Formation/EquipmentIds/SimpleEquipmentIds）本就齐全，磁盘归档也聚合了全字段；缺口只在"UI 只读内存投影、不读磁盘"。本版补上磁盘读取后，已归档对局的阵容/装备/背包即可完整回看。

## 三、投资环境 / 投资策略（备战识别与后台记录）

- 备战界面中间偏上 4 槽（首槽投资环境 + 后 3 槽投资策略）识别已有（`InvestmentIconSlots`，Phase2RecognitionRegions.cs:84-87/220），写入 `InvestmentEnvironmentId/InvestmentStrategyIds` 并随节点记录落盘；
- 玩法依据：投资环境开局固定刷新一个、选后不变（数据/文档确认）；投资策略在 1-3 首获（README/DATA_IMPORT 确认），2-2、3-2 节点与"最多 11 个、获取后不丢弃"为**用户实机经验**（仓库无文档证据，已在代码注释与本文档标注来源）；环境/策略可额外给策略（如"联席决策"2-6、"策略大师"3-5，investment-environments.json 证据）。
- 说明：备战识别的"每次确认新增"依赖备战帧稳定识别（已随帧管线执行）；历史对局窗口可回看每节点识别到的投资环境/策略 ID（名称映射）。

## 四、UI / 美术改进

- 历史窗口列表选中样式：WPF 默认蓝色高亮 → **金色细线 + 微光背景**（科幻感、与深空蓝黑 + 金色/青色强调一致，不花哨）；
- 按钮、列表、滚动条样式在历史窗口间统一（细线边框、圆角 2、悬停金色）。
- 说明：本机无法实机预览 GUI 渲染效果，本轮只做**可验证的一致性改进**；更大胆的视觉重设计（配色方案、动效）建议实机验收后按你的反馈迭代。

## 五、测试与交付

- 新增测试：`CompletedRunsListExcludesIncompleteAndReadsArchives`、`CompletionArchiveCarriesIdentityEvidenceFromCheckpoint`（磁盘读取 + 开局身份落盘验证）。
- 定向回归 38/38；全量回归 **562/562 通过，0 失败，0 跳过**。
- 候选包：`artifacts/CurrencyWarsSmartRaccoon-0.2.760-win-x64-portable.zip`（附 SHA-256）。
- 未实机对局验证（本机无游戏）；"开局信息记录"需你实机跑一次自动刷开局 + 完整对局确认。
