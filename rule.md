# rule.md —— 货币战争智能狸：AI 协作规则与错误全账本

> 新 AI 必读三件套：**本文件**（规则+错误账本）+ `HANDOFF_SESSION_20260902.md`（做了什么/软件背景/游戏机制/关键文件位置）+ 记忆文件（自动加载）。
> 本文件只放规则与错误。违反任何一条=事故；同类错误两次=立即停工报告根因。

---

## 一、最高铁律（红线）

1. **软件仅在用户当场明确下令时行动。** 指令的字面范围=行动边界；绝不因"流程上接得上的下一步"自行串联（出战/弃局/卖人/上场调整/阶段推进都是独立决策节点）。
2. **每一个修改（代码/文档/发布/配置）完成的同一回合，必须当场写入 handoff。** 禁止攒批、禁止事后补记。
   **漏记处置流程（2026-09-04 用户令，最高优先执行口径）**：一旦发现任何改动没有及时记录——**立刻停止手中的一切工作（包括正在进行的修复/审查/发布）→ 先补档 → 补档完成后停下来，等待用户的下一步指示**。严禁补档后自作主张继续接着工作。
3. **同类错误出现第二次 → 立即停止一切操作，报告根因。**
4. **未验证不下结论。** 异常先读事件日志 `%LOCALAPPDATA%\CurrencyWarsSmartRaccoon\logs\test-session-*.jsonl`（Timestamp 本地 +08:00），猜测必须标注"未证实"。
5. **写指令文件=立即发出**（约 700ms 内被消费）。取消 AI 的轮询≠撤回指令——必须核实结果文件回执再下结论。
6. 用户说"停"=全停（含只读操作）。
7. **未经用户允许，禁止发包（覆盖稳定目录/计划任务拉起新版）与实际测试**（2026-09-04 用户令；违规实例 1.2.62/63）。修复代码可随时写入源码与构建，发布与实测必须等明确允许。
8. **方案四点检查单**（每个方案动编码前的必要过审条件，四点逐一详细论证，禁止"已核对"三字带过；任一不过=删码重推）：
   - 立项前先研究既有设计（规格/决策树/记忆/机制数据），提炼设计思想再动方案；
   - ①不违背既有设计思想；②不改用户已拍板/已修复内容；③不严重违背游戏机制；④不违背原指令集/决策树设计逻辑。

## 二、决策层行为规则

1. **变更类指令发前必过前置门**（用 I1/I10 读事实核对，不凭印象）：M1/A4 要求场上≥1 人；A1 显式槽位=确认该位置符合意图；M5 要求备战页；I1 页面帧龄>15s=陈旧帧禁作现状。
2. **游戏不在前台=操作层不执行**（前台守卫）。任何拖拽/点击类指令前，必须先把游戏窗口切到前台（PowerShell SetForegroundWindow 游戏进程），否则指令假死等焦点。
3. **决策层状态判断只依赖软件输出**（I10 完整阵容+装备、指令回执、事件日志），禁止用游戏外截图补盲。
4. **上场凑满羁绊→必弹祈愿→必须 M3 应答后才能继续**（漏发=页面门禁卡死）。
5. 轮询：短间隔、**匹配"完整命令+⇒ 终态符"即完即退**（不得用会漏匹配的短模式空转烧上限）；等待必带上限。

## 三、指令语义规则（编码必守）

1. **指令参数只传语义**（角色名/槽位号/优先级），坐标全部写死在操作层。
2. **卖出类指令=位置语义**：`A2 前台|后台 槽位号(1基)`、`A3 备战席槽位号(1基)`——操作层按标准槽位几何直接拖出售区，**不做名称反查、不依赖识别卡位**（识别卡位带旧布局 Y 残留）。
3. `A1 角色名 前台|后台 [槽位号(1基)]`：显式槽位=精确落位/有意互换（人口满时的合法上场手段），显式模式不推进占用计数。
4. `A4 前台|后台 槽位号(1基)`（2026-09-03 位置语义定稿，角色名形式废除）：把物品栏命运圣杯星徽拖到该槽位**已上场**角色；星徽只装给已上场角色（装备战席=白装）；源点=物品栏全面板最大金色连通簇探测（**探测不到=诚实失败，定标点盲拖已废除**）；拖后重扫面板自证（金簇骤降=成功）；**067 英雄登场局禁发 A4**。
5. M1 期望落地页必须按节点实际填写；**1-3 备战节点禁止 M1**（见四.6）。

## 四、游戏流程规则（用户拍板，机制严格）

1. **1-1**：开金矿(M2)→重新识别→商店→上场→星徽→等令 M1。
2. **1-2**：进入自动弹商店——**先商店购买(M5)→开金矿(M2)→识别→后续同 1-1**。
3. **1-3**：进入即弹投资策略选择（模态，**M7 最优先，不选完做不了任何事**）→选完游戏会自动弹商店（**2026-09-06 用户第4次重申强制令，推翻09-03旧口径：弹出商店必须扫描识别并按白名单购买，识别半帧（含未识别槽）时静置重扫一次，绝不直接关掉不扫**）→**开金矿（M2，矿产出参与决策；含『非常大的金色球』=大金矿球——1.2.108起第二趟检测（扩区+大半径+金盘校验），此前三重致盲从未被检测到=历次『金矿没开』根因；用户2026-09-06拍板大金球必须开，推翻此前『金矿无视』口径）→卖掉冗余角色凑金币（S1A 口径：非命杯/非星徽/非 5 费，保留线=已获星徽数）→之后才开始 M5 圣杯循环找角色**（2026-09-03 用户更正：晶矿→卖→刷，M5 不再打头阵；上场命杯成员+星徽携带者穿插其间）。
4. **金矿（M2）必须先于全部阵容识别**；矿球会稍晚出现，M2 报"无球"后部署决策前要重试；矿产出（角色/装备/金币）必须参与上场决策。
5. **人口状态机（常量，不识别）**：1-1/1-2=3、1-3=4、升 5 人口=购买经验×2=8 金；仅在"有角色可上且人口不足"时买经验。**经验机制（2026-09-03 晚用户补充）**：每过一个节点自动获得 2 点经验——不买经验时 1-3 自然变 4 人口，此时买 2 次经验（8 金=8 点）到 5 人口。
6. **1-3 是长期备战节点（三星五费在此达成）**：备战期**禁止 M1 打 1-3**——打了就离开备战阶段，毁掉整个计划；M1 仅在备战完成且用户明令后执行。
7. **上场凑满羁绊→必弹祈愿→M3 应答**（M3 优先级写死：首祈愿盲选左→奇迹代偿血≥89 必选→令咒侧=聘用书→无限之釜→一般选左）。
8. **星徽装配**：只装给已上场角色（装给备战席=白装）；拿到星徽马上装（时机=上场后）；星徽携带者计入命杯成员。**识别口径（2026-09-03 晚用户拍板：星徽识别只修一件事）**：①物品栏星徽=StarBadgeLocator 模板定位（一定要识别出来）；②非星徽绝不误判；③**角色身上的装备识别漏读由状态机账本代替**——A4 装配成功=本局内该角色恒带徽（星徽绑定角色不绑槽位，换槽随人走；组装器按"识别∪账本"并集取值，识别永不推翻账本），决策层无需重复确认识别；067 局禁发 A4；徽在备战席携带者身上→A1 显式槽位换其上场，绝不重拖。
9. **商店**：绝不刷新——**仅 1-1/1-2 生效**；**1-3 必须刷新且一条指令逛到黑**（2026-09-03 用户最终拍板，商店控件内置逻辑非决策层）：成员<4 或已升5 → 刷到金币不足刷新价为止（无保留线，刷新次数上限仅兜底、绝不允许上限强制关店空转）；成员=4 且未升5 → 金币>10 立即买经验升5继续、≤10 直接停刷收摊；**买角色前金币<费用绝不点**；买到才关店（买→关店→上场→重开）；昔涟买而不上不关店。命杯成员见到就买（同名一次）；银河学者规则**仅 1-1 生效**（购买与上场都只限 1-1；已有 1 名才买另一个不同的；**同帧双学者都买仅 1-1**；2026-09-06 第四次重申后与四.21 统一口径，此处"1-1/1-2"系过时表述——1.2.105 实锤 1-3 场上已有双学者时条件追加分支把真理医生买进并上场）——1-3 白名单=命杯成员+昔涟。
10. **卖出**：仅 1-3 可卖；命杯成员/星徽携带者/5 费绝不卖；卖出后必须 I10 复核（A2 的"OK"只代表输入成功，不代表卖出生效）。
11. **卖出顺序机制**：场上角色先用 A1 显式槽位互换换下（或 A2 直接场上卖），再 A3 卖备战席；卖出前查官方费用（data/4.4），5 费触发禁卖。
12. **067 英雄登场赠体**（随机 2★5费）：19 个节点内禁上、不激活羁绊，永远排除在上场候选外（战斗完胜可提前 1 节点）。
13. **策略/环境优先级（宏内写死）**：策略=彩238>都是这家伙的错>金051>二极管，全未中选最左；环境=英雄登场>命运圣杯邀请。
13a. **策略弃局例外（2026-09-03 用户拍板；此前"绝不弃局"自此有了唯一例外，三条件"且"关系须同时满足）**：①用户目标=**全员三星五费**；②**1-2 阶段识别血量 ≤86**（>86 极罕见，但判定口径就是 ≤86；血量取 1-2 阶段 I10/持有器记录值，决策层带入 1-3 判定）；③最终选定的投资策略**不是二极管**（情形内若二极管已刷出则选中它、继续打不弃——二极管是全员低血局的翻盘命线，此条为按③"刷出"意图的推导口径，如与用户意图不符请纠正）。三条件同时成立→决策层直接发**弃局重刷（A9→M8）**；二极管未刷出时的选择动作随意（组件自主选最左/随机均可，反正结果必非二极管）。**单人模式或血>86 永不因策略弃局**（N11 其余口径不变）。
14. **备战席 1 费卡名字可能识别混淆**（如翡翠被显示成其他 1 费名）——阵容对账以快照已购集合（OwnedCharacterNames）+ 槽位为准，不纠结单帧名字；卖出后以 I10 复核为准。
15a. **备战席武装箱/聘用书箱=特殊占用位，不是空位**（2026-09-03 用户重申）：识别层已有实现（2026-08-17 D 步：简易武装箱=currency_wars_equipment_157、特权=156、进阶=158、3/4 费聘用书=special_item_020/021 模板识别，识别为 SpecialOccupied）——决策层视其为占用：不部署、不卖出、不计入可用空位；I10 备战席名单不含它们=正常。编码时不得当空槽处理。
15b. **GPU TDR 崩溃会引发游戏回档**（2026-09-03 实测：崩溃重启后游戏恢复到旧存档——已卖角色复活、金币回退、盘面整体跳变）。决策层必须能识别回档签名（金/成员数"无故增加"、已卖角色重现）并重新对账；识别读数互相矛盾时先怀疑回档（截图核真）而非盲目重试。
16. **拖拽操作时好时坏的处理**：同一槽位连续失败的（如备战席 5 号位黑塔 3 次命令 6+ 次尝试均失败），重试 2 轮后暂停该操作换路线/上报，不无限重试；失败案例与正常槽位的差异（本例 5 号位）需离线用输入诊断定位后专项修（1.2.32+ 候选）。
16a. **决策层决策速度要求（2026-09-03 用户拍板）**：刷到三星五费概率低=局数就是资源，软件执行速度+未来决策层的判定速度都要快——决策层编码用状态机查表/常量判断，禁止重计算/重 IO；识别等待参数在保住验证判据的前提下取实测可行下限（1.2.29 已砍一轮）。
16b. **多局连刷的事件计数基线**：应用/游戏重启后事件态（祈愿计数等）会归零或继承，跨重启判定"这是第几个祈愿"不可依赖进程内计数——以 I10 现读+游戏面板为准（2026-09-03 祈愿计数跳变实测）。：人口满时拖到有人槽=与该角色互换，是正常操作；需精确落位/互换用 A1 显式槽位语法。
17. **1-1/1-2 满员买到命杯成员=暂时不上（2026-09-03 晚用户拍板，销掉旧悬案）**：坐备战席等 1-3 统一上 4 人口；极端情况 1-1/1-2 就拿到 4+ 名命杯成员同样暂不上。机制依据=四.5 经验机制（每节点自动+2 经验，1-3 自然 4 人口）。1-3 内上场=识别快照真实空槽，全满不拖不互换。
18. **M1 异常诚实包装（2026-09-03 落地）**：战斗组件抛出异常（如 GPU TDR 打挂游戏窗口后的 COM/DXGI「参数错误」）必须包装成失败事实回传（含诱因提示+先 I1 核实），绝不裸抛给通道层；收到此类失败≠战斗失败，先核实游戏状态。所有拖拽类指令 2026-09-03 起带输入诊断（logs/input-diagnostics.jsonl DragDiagnostics 行）——拖拽失败先读诊断再定位。每条指令的下发/结果/耗时落黑匣子 logs/grail-flight-*.jsonl（实机验证以日志为准）。
19. **投资环境页=机制固定一次刷新；商店"免费刷新"是另一回事（2026-09-06 用户澄清修订，取代 09-04 旧口径）**：①**投资环境页机制上固定允许刷新一次**（每轮必有，非"免费额度"概念）——刷开局未命中偏好→**先点刷新 (676,984) 重新找一批**，仍未命中才弃局重开（1.2.101 恢复；09-04 删除系与商店免费刷新混淆所致）；②**商店里名为"免费刷新"的特殊按钮不归本软件管**——节点推进商店自动出新=机制行为非刷新操作；任何付费/免费边界不清的按钮，未核实代价前一律不点（坑 46 精神保留）。
20. **环境命中白名单口径（2026-09-05 用户终审拍板，永久口径，禁止再问）**：命中=仅 **067 英雄登场 / 019 命运圣杯邀请**；**圣杯七变体（命运圣杯契约/量子同频契约等）一律不算命中**——识别判未命中继续刷是正确行为，绝不放行。用户原话："圣杯七变体当然不算命中，我之前已经讲得很清楚了，不要再问这些基础问题。"
21. **出战人数口径（2026-09-05 用户拍板；2026-09-06 第四次重申补全）**：部署成员 **≤2 也照常出战**（M1 内置"人数不足确认"处理器每轮战斗点一次，已有实现）；出战前**尽量多上成员**（M5 买到即上+学者补位），成员太少可能战斗超时。决策层**不设**"成员数≥2"出战门禁（M1 前置门维持"前台≥1"口径）。**银河学者规则只在 1-1 生效（购买与上场都只限 1-1——1-2/1-3 一律不买不上，用户第四次重申禁止再犯）**；**1-3 全员清场（2026-09-06 最终口径）：一到 1-3 节点，除命杯成员/佩戴命运圣杯星徽者/五费角色外，场上+备战席全部卖掉一个不留、不搞其他事情；若 1-3 通过投资策略获得了命运圣杯星徽→少卖一个（保留装配载体）并把星徽装到该角色身上**。
22. **弃局结算页不识别、盲点回主界面（2026-09-05 用户令，第 4 次重申后立规，永久口径）**：弃局路径上的结算中间页族（挑战失败/"对局未完成"C 级总结/任意结算转场页）**一律不做页面识别**——不裁模板、不进识别表、不按页面身份分流；A9 的 Esc/弹框确认链走不通时，直接**盲点中下部推进位（0.5 秒间隔）直到回到货币战争主界面**（分类或强证据确认主界面即停，上限 15 秒）。禁止再提议"给某结算中间页加识别"（历史：1.2.92 加页面识别=方向错误已 revert；正确答案永远是盲点直通）。（例外口径：盛会之星升档选择框属选择类模态非结算页，按四.23 识别应答，不与本条冲突。）
23. **盛会之星羁绊升档选择框（2026-09-06 深夜用户口述机制；22:33/22:49 两次卡死实弹）**：该羁绊**每次升档必弹**金色选择框「请选择1名角色成为巨星」——无论第几次激活/重新激活同一档位都弹；模态吞输入（Esc 无效、识别表外时 I1 只报底层 preparation 族）；**选择内容对软件无意义=纯绊脚石**（用户原话）。应答口径=**随便点一张角色头像→点「确认选择」**。1.2.114 起软件自动应答：弹框入识别表（gala_star_bond_selection）+弃局链兜底/运营循环泵按页 ID 应答消除，全点位限制在弹框矩形内。

## 五、数据与编码规则

1. **名单零手写**：角色/羁绊/费用运行时从 data/4.4 解析（`GetBondMemberNames`）；银河学者 5 人=大黑塔4/真理医生3/阮•梅2/黑塔1/艾丝妲1。
2. **验收=逐字段对规格**（"指令接线 31/31"不算数），且**"已修复"=实机日志有执行痕迹**，不是代码存在+单测过。
3. 一批改动只做一轮验证（构建+短套件 Grail ~105 项 3 秒）；全量测试低优先级跑法不可靠。
4. 事件日志是排障金矿；识别层卡位坐标带旧布局 Y 残留（约 +187px），拖拽类操作一律以标准槽位几何（部署验证过的槽位表）为准。
5. 子代理一次最多派两个；失败要重跑。

## 六、错误与弯路全账本（一坑一行，全部已修复/已定对策）

1. 识别流静默冻结且心跳续传旧帧 → 帧流+新鲜度双看门狗（1.2.10）。
2. 僵尸帧被当现状误判"游戏自动开打" → I1 帧龄标注+陈旧禁用（1.2.10）。
3. M8 停靠点脑补 → 以用户拍板字面为准：命中 067/019→立即选中进局→1-1 备战席停。
4. 槽位计数器账本三次事故（A1 拖上占用槽/M1 空场出战/M5 仪式换下星徽携带者）→ M8 双账本归零+A1 显式槽位（1.2.11/1.2.14）。
5. 同帧双银河学者漏买 → 规则分支补齐（1.2.11）。
6. 星徽装配三重拦截（闩锁/识别失明/旧口径装备战席）→ 动态质心探测+按住 250ms+只装已上场角色（1.2.12）。
7. I10 输出残缺（无阵容/装备明细）→ 快照补全（1.2.13）。
8. 验收只数"指令接线"不核字段 → 验收=逐字段对规格。
9. M5 结果行报买前货架、不报买到谁 → 已修（1.2.20 报买到名单）。
10. 白名单手写漏艾丝妲/阮•梅 → 名单运行时解析（1.2.9）。
11. 想上第 4 人（1-1/1-2=3 人口）→ 人口状态机违反。
12. 流程顺序错（1-2 商店先行；金矿先于识别）→ 用户拍板入档。
13. 上场凑羁绊后漏发 M3 → "上场/购买后必查祈愿弹框"入流程。
14. A4 规格要求目标角色名参数 → 已参数化（1.2.20）。
15. 装备显示名映射缺失 → 诚实报"装备NNN"尾码。
16. "前台区域无角色无法出战"弹框不在识别表 → M1 前置门兜底（弹框识别待办）。
17. 发布版本混淆 → 覆盖+文件夹名同步版本号+删旧。
18. M7 全未命中回"需退出重刷"违背树 N11 → 软门槛开关漏开已补（1.2.15）。
19. 策略选择界面会重现且无法 Esc → M7 循环再选、最新为准、上限防卡死（1.2.15）。
20. A2 场上出售被写成 Fail 桩（组件/坐标/数据全现成）→ 接线（1.2.16）。
21. 识别层 formation CardRegion 带旧布局 Y 残留（+187px）→ 标准槽位几何覆盖（1.2.17）。
22. 两次 A2 报"OK"实未卖出 → 卖出像素差验证（1.2.17）；"OK"≠生效，卖出后必须 I10 复核。
23. A2/A3 按名字反查识别卡位违背位置语义设计 → 位置语义重设计（1.2.18）。
24. **备战期误发 M1 打 1-3**（23:06，16 秒失败退出；阶段差点被推进）→ 铁律四.6：1-3 禁止 M1，M1 仅备战完成+用户明令后执行。
25. 游戏不在前台未先切换就发拖拽指令 → 前台守卫假死等焦点 → 操作前必须 SetForegroundWindow 游戏（本轮实测）。
26. 轮询 grep 模式漏匹配参数段导致空转烧上限 → 匹配"完整命令+⇒"。
27. **1-3 商店循环口径记错且无阶段范围**（把"必关店/绝不刷新/同帧双学者"整体套到 1-3；用户 2026-09-03 指出"之前已经说过很多次"）→ 修正：关店仅在买到角色后（买→关店→上场→重开，无目标不关店直接刷新）；绝不刷新仅 1-1/1-2，1-3 必须刷新（2 金/次查金币）；同帧双学者仅 1-1。教训：**阶段相关规则入档必须带阶段范围**，否则跨阶段套用就是事故（树 N14/N1b、rule.md 四.9、代码 1.2.21 三处已同步）。
28. **exit.txt 残留导致新实例启动即自退**（1.2.21 发布实测：旧实例消费后未删，新实例 LastResult=0 但进程秒退）→ 发布/重启流程第 0 步=检查并删除稳定目录 `指令测试-exit.txt` 残留再拉起。
29. **exit.txt 是空闲轮询消费，写完必须轮询进程真退再删**（1.2.23 发布实测：写完仅等 3 秒即删→旧实例没轮询到→不退→覆盖报 Device or resource busy）→ 正确顺序=写 exit.txt→轮询 tasklist 至进程消失→删残留→覆盖→拉起。
30. **M5 回执金币报指令入口缓存值**（1.2.22 实测：用户看到游戏 13、回执报 19——持有器金币只在备战页识别更新，商店页刷新扣款不回写）→ 1.2.24 修：宏回执行行器循环本地账（LastShopPassGold），刷新扣款实时反映。
31. **刷新次数上限强制关店=变相关店空转**（1.2.22 实测：每条指令刷 3 次就收摊，用户看到"刷两次就关店"怒斥离谱）→ 1.2.24 修：上限改 40 仅兜底，真实边界=金币耗尽；一条指令逛到黑，关店仅发生在①买到成员②金币不足刷新价③4 档≤10 金三处。教训：**"不关店"语义必须覆盖上限/异常等一切中途退出路径**。
32. **误判"5 费保护"未查数据**（把阿格莱雅 5★稀有度当成模式内 5 费而当保留理由；实测 data/4.4 costs=[1]）→ 铁律：5 费判定唯一口径=运行时查 data costs=[5]（IsPureFiveCost），**稀有度≠模式内费用**，绝不凭印象/标签。
33. **把 22 人名单误用作卖出保留规则**（名单=上场候选优先级②，与卖出保留无关；保留数量唯一依据=已获星徽数，S1A"留哪个都可以"）→ 两条规则各管各的，编码时禁止交叉引用。
34. **识别已证明不可靠时仍按识别标签下单卖人 → 误卖星徽携带者娜塔莎**（本局识别多次把娜塔莎读丢/与爻光重名；我却按 I10 标签"F3=阿格莱雅"发 A2，事后娜塔莎[徽]与阿格莱雅双双消失、金+4 与两者卖价吻合——一条 A2 卖出两人的机制未完全定位，部分未证实）→ 铁律：**卖出前身份必须双源确认**（截屏/装备识别，识别标签不可靠时尤其如此）；卖出后 I10 复核是硬性收尾、不可中断省略。缓解：误卖星徽携带者=徽章退回物品栏可重装（A4），非永久损失。
35. **纯颜色探测无法区分星徽与其他金色物品**（1.2.25 实测：A4 全面板探测把其他金色物品当星徽拖上爻光，自证被"面板金色总量减少"骗过谎报 OK；审查 P2-1 警告成真）→ 教训：**颜色/像素探测判得了"金色"判不了"是星徽"**——修复方向=识别层星徽区域（I7 InventoryStarBadgeRegions，形状感知）作主源+颜色探测降级为兜底；修复前 A4 禁用于实战。颜色误拖的杂物已装无法卸下（机制未知），不影响羁绊计数但污染装备栏。
36. **角色装备识别"多件只认第一件"回归一直未修，I10 徽章漏读是它的症状**（2026-09-03 晚实测复验：prep_3_7 期待装备 [066,083,066] 实得 [066,null,null]、证据候选名单漂移、猎星人标签/商店等级漏识别；角色识别本身 11/11 全对——"08-20/21 标定重做"引入，Skip 守卫测试一直在真败）→ 用户拍板：**装备识别只修星徽一件事**；角色身上的漏读用状态机星徽账本代替（A4 成功=本局恒带徽，识别∪账本并集，2026-09-03 落地）；其余装备漏读由决策层防御口径吸收（升档真值源=弹框事件，不硬依赖 I9/I10 装备明细）；Skip 守卫保留待独立批次专修。
37. **测试台路径 M8 从不复位持有器**（2026-09-03 晚代码核查发现：GrailRunLoop 生产路径每局 Reset，但指令测试台的 M8 只清执行器计数——已购集合/事件态/账本跨局残留，第 2 局会带着第 1 局"已拥有"假象拒买真目标）→ 修：M8（对局边界）统一调 stateHolder.Reset()（GrailMacroCommands.RunOpeningAsync，1.2.32 批次落地）。
38. **盲卖连发误卖远坂凛（坑34 同类错误第二次，2026-09-03 21:54 实测，AI 决策层停工）**：1-3 卖冗余时连续 3 条 `A2 前台 1` 之间只靠**滞后的 I10** 复核（阵容压缩/矿掉落/换人动画期间 I10 给出过期盘面），且首卖后出现"回执失败但金币增加+卡消失"的反证仍未停手；远坂凛（命杯成员）在压缩位移中走到被卖槽位被卖出（用户目击+羁绊面板"命运圣杯"行消失证实）。教训：**①位置语义卖人必须"一卖一截图"双源确认，I10 在动画期只作参考不作依据；②卖出类指令出现任何反证（回执与盘面矛盾/金币对不上）=立即停止卖出序列；③场上存在识别被证明不可靠的卡时，禁止一切连发卖出**；④软件侧 A2 回执在动画期不可信（Fail≠没卖出）、I10 帧滞后于盘面动画——两者都是已立案缺陷。缓解：凛=1费可回购，羁绊可恢复，本局无永久损失。
39. **通用弹窗解除的 ✕ 盲点击把货币战争模式关回游戏主界面（2026-09-04 05:40~08:07 卡约 2.5 小时）**：决策层夜间批次的 DismissBlockingPopupsAsync 盲点右上 ✕(1860,64)——那是指南页关闭钮，每轮循环点一次=每轮把 CW 模式踢回 normal_hud，M8 再导回来，形成踢出-回环循环（首次确认 normal_hud=06:10:06，实际踢出约自 05:50 起）。**复合失误**：①该批次夜间未走子代理审查；②修复代码写入源码后部署命令被取消，未重新执行——带修复的 1.2.48 迟至 08:09 才上线；③监控只打印"变化行"，循环被去重掩盖。教训：**新增任何"盲点击"必须先回答"这个位置在别的页面是什么"**；修复完成后必须确认部署成功（部署取消=重新执行）；监控不得用去重掩盖重复异常。1.2.48 修复：解除逻辑只点空白(960,540)推进位面图，✕ 点击已删除。
41. **把游戏主界面（normal_hud 的 3D 世界形态）误判为异常态，引擎在其上空转 4 小时（2026-09-04 凌晨~晨，用户晨间质询定性）**：游戏主界面的真身=3D 世界场景（朝露公馆圆桌+"货币战争"F 交互提示+左上指南按钮）——快速分类器早已正确判出 normal_hud 0.638，A9 弃局回执"已返回货币战争主界面"也证明这里是弃局合法落点+每轮重刷的正常起点。**我却在两层连续犯错**：①心智模型错误——把"主界面"固化为 2D 主屏截图，不认 3D 世界形态，把正常态当"需要退出的卡点"；②引擎在正常态上叠加 A9 弃局（无局可弃=失败）/世界内盲点走位/Dismiss 等破坏性操作 4 小时。教训：**①页面身份判定以分类器结论为准（normal_hud=主界面=合法起点），人不得凭 2D 截图臆测 3D 界面身份；②引擎入口决策树：I1 判页→normal_hud=直接 M8（导航流自带主界面→玩法界面方案，勿重新发明）；preparation_=直接续打；仅 Unknown=解除尝试；③A9 仅在"确实进过局"（局内事件标志）后允许，主界面/无局时 A9 必失败属正常，跳过即可**。1.2.49+v3 引擎已按此修正。
42. **Unknown 页导致的弃局/异常终止=软件问题，零容忍放行（2026-09-04 用户拍板）**：任何"识别 Unknown→无法继续→弃局"一律视为软件缺陷（识别表缺口/迁移候选表缺口/流程缺口），禁止放行为正常。实锤两例：出战迁移不认"前台区域无角色"提示→9 局报废（1.2.58 修）；1-3 胜利结算页被 challenge_health_depleted 误判为败北→2 继承局胜局被弃（待修）。
43. **M1 前置门部署动画期误判（X13 违反）**：部署成功后立即 I10 检查会撞部署动画+识别滞后→误判"前台无人"→弃掉已部署好局（19:17 局实况）。修=检查前等 3 秒+未找到再等 3 秒双读（1.2.61）。蓝图 X13"部署回执后等 3-5 秒再核对"必须落实。
44. **拖拽 holdMs=0 生效不稳定**：同参数拖拽一成一败（19:23 远坂凛成功 vs 19:31 吉尔伽美什"前台无角色"，诊断输入完全一致，X4 立案更新）→部署/出售拖拽加 MouseButtonHoldDelay=250ms（星徽装配 1.2.27 实测可靠参数，1.2.62）。
45. **1.2.58~1.2.63 五批次发布前零子代理审查（坑 40 同型复犯，2026-09-04 20:0x 用户审计）**——以"用户在等"为理由牺牲审查纪律。补救=综合补审子代理覆盖全部未审改动。审查纪律不可为速度让步（坑 40 原教训，再次生效）。
46. **把"免费刷新一次"当成固定机制写进刷开局（2026-09-04 晚用户审计定性；2026-09-06 已修订）**：09-04 定性时把投资环境页的**机制性刷新**（每轮固定允许一次）与**商店的"免费刷新"按钮**（特殊机制）混为一谈，据此删除了环境页刷新（1.2.67）。09-06 用户澄清两概念不同后 1.2.101 恢复环境页机制刷新（未命中→点 (676,984) 重找一批）。教训保留：**付费/免费边界不清的按钮，未核实代价前一律不点；机制入档必须区分"机制允许的刷新"与"免费刷新操作"——概念混淆会错删正确功能**。
49. **多批次漏子代理审查第三次复犯+过时基线口径容忍（2026-09-05 晨用户审计定性，坑 40/45 同型第三次）**：1.2.72~86 十余批次中 1.2.77/78/79/82/83/84/86 等七批发布前无独立子代理审查（1.2.79 发布后才发现补审），其中 1.2.85 盲点守卫补审 FAIL（时序漏洞）后回滚——漏审流程的代价真实发生；同时全量测试"环境敏感清单 ≤12 项"的基线口径已过时（用户已修复全部基线失败），26 失败被错误容忍未及时查明。教训：**审查纪律不可为速度让步的第三次数训——每批完成立即审查，不攒批；基线口径以用户最新声明为准，过时口径必须当天更新**。
48. **识别表新增页面须同步三处，漏一处=页面判定静默失效（2026-09-05 凌晨断线自愈实装时实锤）**：新增页面 ID 需同步 ①JSON 识别表（page-recognition.1920x1080.json，全量分类器）②AutomationPageIds.Ids（指令层子集分类器）③Phase2RealtimeRecognitionPipeline.FastPageIds（管线 fast 集白名单）——漏③时 fast 报"未知"覆盖全量识别结论，页面判定静默失效（I1 一直报未知，断线分支永不触发）。同型历史残留：preparation_1_1/1_2 曾在 AutomationPageIds 而无 JSON 定义（反向漏配，永假条目）。
47. **监控误报式恐慌：把"弃局恢复的过路选择"当成"软件接受不合格开局"（2026-09-04 晚，同晚第二例）**：用户看到日志"选择投资环境：轮岗"质问轮数上限——深查后真相=环境页是强制模态（不选不能进局），弃局恢复必须选一个环境进局再走局内弃局，选第一槽只是过路动作；决策层轮数上限本为 null（无限刷）。教训：**看到反直觉日志先查"这个动作在弃局/恢复流程里是否必要过路"，再下结论；向用户报告前先完成全链路定位（本次用户连发两条质询才拼齐真相）**。
40. **夜间批次集体跳过子代理审查 + 部署失败串链继续（2026-09-04 00:18~08:07，用户晨间审计定性）**：00:18 最后一次子代理审查后，连续 9 个版本（1.2.40 自动复活/1.2.41 决策层引擎/1.2.42 节流豁免/1.2.43 指令超时/1.2.44 分级超时/1.2.45 世界内点击+A15别名+A9语义/1.2.46 交互键/1.2.47 结算连点/1.2.48 ✕删除）零审查，其中 1.2.44/45 两次部署在 build 失败后仍串链继续执行（robocopy rc=16 被吞）。**根因=以"速度"为理由牺牲审查纪律**；后果=决策层引擎带着未审缺陷上线（幸被监督弧兜住）。教训：**审查纪律不可为速度让步——跳过审查的批次=不可信批次**；部署失败必须中止串链；每批完成后立即对账"本批审查是否完成"。补救=补发三组审查（引擎/复活/结算交互），FAIL 全部处置后才继续生产。

50. **备战页浮模态弹框 Esc 无效→弃局链 5 连败→引擎自保停机（2026-09-06 深夜 22:33/22:49 两次实锤；首个 DECIDE 录制视频抽帧定性+用户 23:0x 口述纠正身份）**：浮框=**盛会之星羁绊升档选择框**（rule 四.23；上一班初判"角色详情弹框"系误判——不是点角色卡弹出，是羁绊升档自动弹出，修法"点空白关弹框"随之作废）。病理链：识别表无此页→I1 只报底层 preparation 族→Esc/退出钮/盲点直通全被模态吞掉→弃局 5 连败→自保停机（22:49 重启后原样复发一次）。取证：Recordings/grail_decide-20260906-222246.mp4（10.8 分钟）+artifacts/badge_forensics/stall_end.png（已入库测试夹具 PageReplay/gala_star_bond_popup_stall_20260906.png）。**修复（1.2.114，已实装+单测 17/17）**：①弹框入识别表 gala_star_bond_selection——标题边缘模板（350×48，edgeDetection），标定正样本 1.000/0.896（两次卡死帧）、负样本 497 帧（当日 run 截图 26+用户实况录像 471）max 0.706，阈值 0.78；坑 48 三处同步（JSON+AutomationPageIds+FastPageIds，fast 集含反射守卫单测）。②弃局链兜底按页 ID 应答（任选角色+确认选择；5 候选点位覆盖 1~4 卡布局、全在弹框矩形内不落底层页；消除失败如实 Failed 绝不 fallthrough 点 (960,899)）；盲点直通遇该页先应答、关不掉急停。③运营循环两处泵（主循环+opening 弹框泵）与祈愿同槽应答。④监听器：弹框在屏期间保持上一备战快照不刷决策 tick。

51. **值守 AI 停工等用户指令浪费整段值守窗口（2026-09-06 深夜 22:0x 用户常设令定稿；此前多班实况："说继续却停下等确认"、每修一批停下等发布批准，夜间空窗反复发生）**。**对策（用户常设指令，运行态最高优先，取代此前"先报等批准"）**：①永续循环不许停——单回合内 sleep≤4 分钟+持续监测（test-session jsonl 增量+result.txt），不许结束回合等指令；②监督必须流程推理（对照 rule 第四节机制+游戏常识+handoff），不能只看报错计数；③发现问题→取证→修复→子代理对抗审查（P1/P2 当场修）→构建 0/0→Grail 短套件→commit→deploy_stable_1271.ps1 发布→DECIDE 重启→继续测，全程不等用户批准；④仅"需用户拍板"事项才问；⑤证据帧（badge/battle/mine-evidence、Recordings 视频）对账完即删省磁盘；⑥任何修改当回合写 handoff 并 commit。**适用边界：仅货币战争运行态值守班；其他项目/一次性检查任务仍走"先报等批准"**。

## 七、发布与版本规则

1. 发布=覆盖稳定目录 `CurrencyWarsAssistant-测试台-当前`（路径永不变）+ 版本号同步 Directory.Build.props + 删除更旧测试台文件夹；正式包 1.2.x 留档不经允许不动。
2. UAC 自治：`register-elevated-task.bat` 已注册计划任务 CWSmartRaccoonCmdTest（用户最后一次 UAC）→ 之后 `Start-ScheduledTask` 免提权启动、`指令测试-exit.txt` 遥控自退。
3. 源码冻结备份：桌面 `CurrencyWarsAssistant-源码备份-重要节点-20260902\`——AI 任何上下文禁止修改/删除/移动。

## 八、监督断言清单与推理纪律（2026-09-05 用户令：五个实锤问题同源定性的制度化整改）

> 背景：1.2.89 前的值守监督"零异常"放行了五个规则违反型问题（学者顶掉命杯/单学者上场/命中局瞬弃/祈愿死锁/R3 误判死）。
> 共同根因=**验证只覆盖机制存在性与事件自洽性，不覆盖规则符合性与游戏语义**——执行成功≠符合规则，
> 软件自我报告≠游戏真实效果，计数器为零≠没有问题。本节是制度化整改，对后续所有值守 AI 生效。

### 8.1 六条推理纪律（监督与报告的必经步骤）

1. **状态对规则核验，不核验事件自洽**：每次监督先从日志事件序列**推演盘面状态**（谁在场/羁绊几/金几/可卖几），再对照规则断言。禁止只核对"某事件出现了"。
2. **回执≠游戏效果**：每条回执描述的是执行，不是效果。任何部署/按键/拖拽成功后，必须回答"这个输入在当前游戏状态下产生了什么语义效果"（拖到已占槽=互换；Esc=状态依赖多义键：对局页开弹框/弹框上关弹框/主界面退模式）。
3. **输入重试前必须重新推导语义**：状态可能已因上一次尝试而改变。禁止"失败了就再按一次"的无条件重试。
4. **计数器为零≠没有问题**：先定义"这类问题的信号长什么样"，再相信计数器。规则违反型问题零异常产生。
5. **终局判定必须审计推理链**：软件（或 AI）从前提推结论的每一步都要核蕴含关系——"拒绝卖"不代表"无可卖"、"I1 报主界面"不代表"真在主界面"（可能陈旧帧）、"没见过开局页"不代表"没经过开局页"（识别流可能漏帧）。
6. **规则文档每条"必须/绝不/仅当"都要转成一条可从日志判定的断言**；转不出来的（缺数据）=监督盲区，要么补识别要么显式标注盲区。

### 8.2 不变量断言清单（值守监督每轮必查；违反=最高级信号，即使零异常）

1. **出战前**：前台人数 = min(当前人口, 可上场单位数)；空着出战=违规。
2. **羁绊守恒**：羁绊数 = 已上场命杯成员数（按买/部署日志推演）；买到命杯+部署后羁绊不增=有单位被换下或没上场。
3. **学者三门槛**：上场必须同时满足 节点=1-1 ∧ 不同学者≥2 ∧ 目标槽=空；1-3 场上学者必须下场并卖出（星徽携带者除外）。
4. **槽位不变量**：任何 A1 部署的目标槽在部署时刻的快照中必须为空，且该快照必须晚于最近一次盘面变异（M5 购买/A1 部署）。
5. **R3 前提**：可卖单位=0（从备战席+场上明细推演，不是从 sold 计数）∧ 金<刷新价；"拒卖"不构成"无可卖"。
6. **模式边界**：回到货币战争主界面后，后续任何输入前必须重新验页；主界面上发 Esc=违规（会退出模式）。
7. **弃局闭环**：弃局序列结束后、任何新操作前，页面必须已确认回主界面；未确认就继续=违规。
8. **弹框优先**：任何输入被吞/失败时，第一假设=有模态弹框在屏，先识别弹框并应答，再继续原操作。

### 8.3 监督报告格式强制

值守报告必须包含：①不变量违反清单（哪怕为零）；②本时段所有终局判定的推理链审计结果；③计数器数据（次位）。只报计数器不报断言核对=监督未做。
