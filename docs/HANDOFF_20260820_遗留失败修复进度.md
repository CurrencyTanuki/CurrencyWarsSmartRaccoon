# 2026-08-20 修复进度记录（银狼放弃 + 遗留失败修复）

## 用户指示（2026-08-20）
- 银狼装备坐标修复方向**放弃**，用户要求回退本轮银狼诊断探针/截图。
- 除银狼外，继续修其它上轮遗留失败。

## 已完成
1. **清理**：删除本会话银狼诊断探针（Silverwolf*.cs、Prep37SilverwolfReviewProbe.cs）与 tools/diag/sw_*.png 截图，git status 无 silver 残留。
2. **IconCatalog 模板计数**（Phase2OperationalCollectionTests.IconCatalog）：断言 9→10(simple：+045__live 生命之花)、70→71(advanced：+080__live 追逐星辰)、35→36(special-item：+special_item_010__live 冶金炉)——均为本会话已交付修复新增实机模板，数据契约同步。
3. **物品栏全空根因C**（Phase2OperationalScreenshotAnalyzer.ObserveInventory）：`slots` 由 `leftColumn.Concat(rightColumn)` 改为 `rightColumn.Concat(leftColumn)`（右列主序在前），并新增 `displayValues = values.Take(rightColumn.Length)`（InventorySlots 显示=右列 7 槽）；左列仍通过 `ProjectInventoryIds(values)` 补全 Simple/Special ids（冶金炉等特殊物品不丢）。
4. **Registry UI 字段覆盖**（HistoricalUiFieldCoverage）：InventorySlotState 补登 `Quantity` 属性（comparted 时已给 InventorySlotState 加 Quantity int?）。

## 验证
- IconCatalog 聚焦测试：通过
- LivePreparationInventory 聚焦测试：通过
- Registry（Cover+NoDuplicate）：2/2 通过
- ExplicitWarmUp / RecognitionOnlyModel：单独跑 8/8 通过（全量失败为机器性能抖动，非本轮引入）

## 剩余失败（全量 26 需在稳定负载下重测；正常负载约 16 项真实失败，含已修 3 项）
可见根因清晰项：
- VisibleStandardEquipmentDoesNotCreateUnknownOrPendingEvidence：Equipped 061 的 CandidateEquipmentIds 空（期望 [061,100] 共享候选），dHash margin≥4 判独占 → 候选空。需确认模板数据 061/100 是否应保留共享候选。
- UnclassifiedSettlementPredecessorSurvivesQueueStaleDropAtPreparationBoundary：帧队列 DropStaleFrames 时序，期望 settlement 保留，实际 dequeued sequence 错位。
- PreparationReferencesExposeNodeDifficulty(125924:1-8/120、132307:2-4/123)：参考帧难度/阵识别。
- Registry、物品栏、IconCatalog 已修。

复杂识别/商店/特权/性能项（需逐项根因）：
- LiveCapturedBattleFrames(伤害OCR)、VisibleShopEquipment、VisibleStandardEquipment、VerifiedPrivilegeFrame、ExpandedShopAnalyzer、KnownSpecial、UnknownFormationEvidence、RecognizesFormationEquipmentAndSynergiesFromRealFrame、LiveCapturedPreparation(性能)、RepeatedPreparation(性能)
- 全量测试在机器负载高时性能类失败会误报增多，需低负载重测。

## 待办
- 继续逐项 root cause 剩余失败（每项聚焦测试验证 + 最后稳定负载全量回归）

## 性能优化（2026-08-20）
- 目标：RepeatedPreparation（132307 全量帧）从 6-7s 恢复到 2s 内。
- rust 审计：基线 1ddd0c3(0.2.842) 该测试=1.79s 通过；当前 HEAD=5.2-6.25s 失败。回归源=738133f「从0.2.855搬入识别层」在 prepare 全量识别里新增了 progress/血量/人口/商店Lv/特权/装备等串行 OCR 步骤。
- 关键分解（当前 HEAD 修前）：total5.25s ≈ formation2.8 + progress0.95 + named0.56 + 其余——各 OCR task 用 MeasureAsync(async)但内部先同步 CPU OCR，单线程串行累积；仅 formation 用 Task.Run 真并行。
- 修复（不改识别结果，只改调度）：
  1) progress/node/difficulty/interest/cumulativeSpend 五个 OCR task 改用 Task.Run 并行（_uiDigitRecognizer 用 ConcurrentDictionary 线程安全已确认；textOcr/WOCR 支持并发）。
  2) RecognizeBoardWithBackRowPerspective 内 front 与 back（含 warp）改用 Task.Run+WaitAll 并行（recognizer 模板缓存 ConcurrentDictionary，只读 frame）。
- EnableTimingDiagnostics 由 static readonly 改为动态读 env（纯诊断改进，无识别副作用）。
- 效果：RepeatedPreparation 6-7s → 2.9s。CharacterCardRecognizer/IconCatalog 9/9 无回归。
- 剩余：距 2s 差 ~0.9s。formation 并行后 1.61s；total2.9s≈formation1.61+node~0.7+progress0.63+named0.37 仍有部分串行（node iter2 异常 702ms、named/equipment 等）。下一步方向：formation 内部 front 多槽并行（Recognize 的 for→Parallel，识别器已确认线程安全）+ 排查剩余串行叠加段。

## 性能优化·续（RepeatedPreparation 6.3s→2.82s）
- 追加并行：CharacterCardRecognizer.Recognize 多槽 for→Parallel.For（模板 ConcurrentDictionary、只读 normalized/frame 线程安全；诊断字段 LastExact/LastDecisive 改 Interlocked+Volatile backing field）。金标准 CharacterCardRecognizer 8/8 无回归。
- 最新 perf 分解：total2.83/2.98s，formation1.49/1.51s，progress0.6，named0.38，node~0.2，equipment0.22。named/equipment 仍是串行尾（ObserveInventory 在 formation await 之后串行执行）。
- RepeatedPreparation 实测 2.82/2.91s（原 6.25/7.31s，下降超 55%）。距 2s 差 ~0.8s：剩余需把 named+ObserveInventory(equipment) 也并入并行（收益递减，涉及依赖关系，风险升高）。
- git 可回退：本性能改动集中 2 文件（Phase2OperationalScreenshotAnalyzer.cs / CharacterCardRecognition.cs）+ 测试断言。回退用 `git checkout -- <file>` 即可回本会话改动（若与其它未提交改动混合需谨慎，建议先 git stash）。

## review 子代理检验（性能改动，2026-08-20）
- 结论：warn，无阻断。三处并行（OCR Task.Run / Recognizer front-back 并行 / CharacterCardRecognition 多槽 Parallel.For+Interlocked+Volatile）全部安全：无数据竞争、无未定义行为、槽位结果按 index 正确、无死锁。OpenCV Mat 每迭代新建 + 模板 ConcurrentDictionary；WindowsOfflineOcr 已内置 SemaphoreSlim.maxConcurrency 多 lane 并发设计；_uiDigitRecognizer ConcurrentDictionary 缓存。
- should-fix（均为上一步早已交付的改动，非本论性能）：①774 应援前置恒空集（有意设计，覆盖变窄需回归）②2596 装备 fixedSlots y 公式与段边界注释矛盾。
- nit：LastExactComparisonCount 并行下非确定写入（仅诊断，不影响结果）。
- 结论：本轮性能并行改动通过子代理审查，无回归。
- 性能现状：RepeatedPreparation 6.25→2.82s。距 2s 差 ~0.8s。限制：ObserveInventory(equipment)依赖 formation 的 benchSpecialItems→逻辑串行链 formation1.5s→equipment0.22s 无法并行；formation OpenCV 1.5s 物理硬成本。再压需改识别逻辑/减识别（高风险需授权）。

## 061 候选装备 bug 归属判定（实证，2026-08-20）
- 用 git stash 隔离对比：HEAD(73b1a23，本会话所有未提交改动前) 跑 LiveCapturedPreparation：
  `Front[0] slot0:Equipped/currency_wars_equipment_061/0.950/[]`（耗时1897.5ms）—— CandidateEquipmentIds 同样为空。
  当前代码(live) 同样 `slot1:Equipped/061/0.950/[]`。
- **结论：061 候选装备列表为空是原本就有的 bug（与性能并行/物品栏改动无关，非本会话引入）。** 本会话未改 Phase2IconRecognition.cs（候选逻辑所在）。
- 附带发现：HEAD 时是 slot0 持有 061，当前是 slot1 —— 槽位索引偏移是另一处差异（遗留，待查）。

## 识别失败项逐一定性+操作（2026-08-20，用户逐条拍板）
- **②VisibleStandard、⑥⑦PreparationReferences、①LiveCapturedPreparation**：候选/待补相关断言是"测试期望过时"——用户定性：主装备已精确识别，候选空≠识别错。已**删除候选/待补强制断言**（保留主装备 EquipmentId 断言）。②⑥⑦已转绿。
- **③VisibleShop、④VerifiedPrivilege、⑤ExpandedShop**：商店当前设计=跳过前方识别，测试期望是改方案前遗留 → **三个测试已 `[Fact(Skip=...)]` 暂停**，注明待下一版"实现商店页识别前方+顶部5槽循环识别"后启用（用户把商店5角色循环识别记入下一版计划）。
- **⑧伤害**：用户确认不动（单位全是"万"，怕个位误加万）——不加代码。
- **⑨prep37**：用户说别动。
- **⑩合成帧逻辑（结算清帧/特殊单位）**：待排查。
- **①物品栏新差异**：LiveCapturedPreparation 转成 inventory 断言失败：软件=[拆装扳手153,折叠小刀044,生命之花045]，测试期望=[044,045,空]——软件多识别出拆装扳手(153)在slot0。待确认是否软件对or测试期望过时（勿乱动，与用户验收过的物品栏排序相关）。

## 装备识别根因定位（2026-08-20 实机像素验证）
132307 帧逐槽 isBlue/非蓝 占比（`isBlue = B>R+25 && B>G+10 && B>70`）：
- Aglaia slot0=83%非蓝 / slot1=61%(轮滑鞋) / slot2=81%
- Sunday slot1(永动机)=68%非蓝 → **dHash conf≥0.90 命中,识别对**
- 千冶刃(后排)B5 slot0/1/2=1-3%非蓝 → 段检测阈值30%远超→后排无段; 财富宝钻也是偏蓝,固定槽dHash conf<0.90被滤 → **后排装备全漏**
- 结论：①装备 dHash 固定槽 `conf≥0.90` 硬阈值误滤"偏蓝/淡蓝/紫系"图标(轮滑鞋046、财富宝钻086)；②后排装备带几乎全蓝(非蓝1-3%)，段检测完全失效。
- 与永动机对比：永动机 dHash 能上0.90所以识别——证明非"蓝面积"，而是"灰阶对比/色泽与背景区分度"决定 conf。
- 用户反例正确：永动机蓝面积更大却被识别 → 纯"蓝色系"不是根因，根因是"跟背景区分度低→dHash conf不足0.90"。

## 后排角色 unknown 回归隔离证据（2026-08-20，用户质问"是不是又改坏后台识别"）
- 用 git stash 隔离：HEAD(73b1a23，此行性能/物品栏改动之前) 跑 132307 帧后排 → Back slot4 `unknown-formation-unit`（角色未识别）+ 装备 unknown。
- 当前代码同样后排 unknown。**结论：后排角色识别成 unknown 在 HEAD 就存在，非本轮性能/物品栏/装备改动引入**。消除"用户担心这次又改坏"。
- 但确实是"后台识别在更早版本就已退化（HEAD 已坏）"，需定位是 738133f(0.2.855自动搬入) 或 A1 批次哪次改动导致。用户在 0.2.839 时代确认过"后台识别正常"（000032 后台 4/4 对），此后某改动回归。
- 待办：用 git bisect 或逐个候选 commit 对比 132307 后排 unknown，定位回归点。

## 人口/OCR 稳定性修复（2026-08-20，用户拍板"哪些还在用 Windows OCR 都换"）
- 根因：人口识别用 Windows 内置 OCR（`ocr`）跨环境不稳定→把"4"读成"5"/读乱码；商店Lv 早已按用户 2026-08-17 定稿改用 PpOcr（_storeLevelOcr）。人口没一起切→不稳。
- 修复：ReadPopulationAsync 从 ReadTextAsync(ocr/Windows) 改 ReadTextRobustAsync(_storeLevelOcr/PpOcr)。效果：prep-1-3-gold、run-171955-prep-1-3 从"Unknown乱码"修复为正确读出 4/4；prep-1-4/1-7 读 5/5（用户确认这些帧本就是5）。人口/商店相关 13 测试通过。
- 顺带：羁绊激活次数(3783) + 图标降级(3829) 也用 PpOcr；6461/6830 数字 fallback 因 `!_enableRobustFallback` 生产不触发(死代码)，已回退不改；图标降级冗余 fallback 已删。
- review 子代理全局检查：结论 warn 非阻断。并行无 UB、OCR 切换无误改。should-fix 已处理：冗余 fallback 删除、无效数字 fallback 回退。剩 should-fix：应援 `cheeredBoardIndices` 恒空押 hasJoyTrailblazer（历史遗留，涉及应援核心暂不动）。
- 遗留（非本次PpOcr引入）：①LiveCapturedPreparation 物品栏差异——软件=[拆装扳手153,折叠小刀044,生命之花045]，测试期望=[044,045,空]（软件多识别拆装扳手，之前已标记待用户定）；②battle-1-4 伤害 16335000vs16205013（用户说不改）；③合成帧逻辑；④RepeatedPreparation 2s 未到。

## 羁绊识别禁用 + OCR 稳定性处理（2026-08-20，用户拍板）
- 用户定：阵容识别已足够稳定，羁绊只从阵容推算（ComputeSynergiesFromFormation + MergeFormationAuthoritativeSynergies 阵容权威）。**禁用羁绊的图标/文字 OCR 识别**（synergyTask 恒空，不跑 ObservNamedContentAsync synergy，代码保留不删）；无阵容只有羁绊意义不大。BondTier/Synergy 测试通过。
- 羁绊(3783)/图标降级(3829/3830) 已从 PpOcr 回退 Windows OCR（避免 Prep 帧被 PpOcr 拖慢）。
- 性能隔离验证：Prep(1-7) 全 PpOcr 时 7-8s → 羁绊/图标回退 Windows 只留人口 PpOcr → ~3.0s（仍略超 3s 预算，疑为机器负载+人口 PpOcr 残留）。人口(6060) OcPcr 仍保留待用户定（建议双引擎：Windows快读→PpOcr兜底）。

## 装备识别根因细化（2026-08-20，132307 帧逐槽 dHash 诊断）
- **财富宝钻(086)**：位于后排 Back idx2 slot1（f0.34）。dHash 候选=[086] **正确**，但 conf 低(IsKnown=False, 0.000)未达 0.90 Known → 被过滤 → 漏识别。→ **降低装备槽 dHash Known 阈值(0.90→~0.72，配 runner-up 分离防误报)可修复财富宝钻。**
- **轮滑鞋(046)**：位于阿格莱雅 Front slot0 slot1(f0.39)。dHash 候选=[045(生命之花)]，**不含 046** → dHash 把该槽认成 045 而非 046，是 **046/045 模板区分度问题**（非单纯阈值）。降阈值会误认 045。需查 046 vs 045 图标相似/该槽照片。→ **轮滑鞋不能靠降阈值修，需处理 dHash/模板区分。**
- 重启确认：当前332307 中阿格莱雅 slot1 空(漏46)、Back 千冶刃装备全空/角色 unknown(漏财富宝钻)。

## 阿格莱雅轮滑鞋(046) 漏识别根因确认（2026-08-20，软件真实路径 EquipSeg + dHash 分析）
- 软件对 132307 阿格莱雅(Front slot0) 真实输出：matched=[055@0.95, 051@0.95, 055@0.95(重复)]，final=2(055,051)，**2号位=Empty(轮滑鞋046漏)**。段路 segSegments=3（其中两块判成055），固定路2号位候选=[045]但 conf<0.90。
- **坐标没坏**：1号位(055反重力皮靴)两路命中、3号位(051光速螺旋桨)段路命中，坐标准确（用户判断正确，之前准）。
- **真正根因**：dHash 汉明距离 **045 生命之花 vs 046 轮滑鞋 = 8/64（几乎一样，区分不开）**。045 是物品栏物品，不是角色装备；dHash 在装备槽2号位把轮滑鞋认成"更像的045"，conf<0.90 未达 Known → 输出空。
- 教训重申：**别再动装备槽坐标**（用户多次强调已改坏、这次实测坐标确实是准的）。问题在 dHash 模板区分度（045/046 靠 dHash 分不开）。
- 修复方向（待用户拍板，未动代码）：①装备槽匹配池排除"物品栏类目0045"；②增加颜色/边缘等更强区分特征（防045/046混淆，可能影响其他装备需回归）；③换更强感知哈希。阈值非重点（用户已否）。

## 轮滑鞋(046)根因第二步：坐标确认 + 距离反证（2026-08-20）
- 132307 帧实际 **2559×1439（2K）**，非 1920×1080。软件 fixedSlots 用 `/1920→ToPixels(帧宽)` = **等价比例缩放**，坐标实际正确（fixedSlot1 px=(974,616,45,48)——从 analyzer 真实 DIAG 拿到）。之前怀疑坐标错位是误判，坐标没坏（验证用户判断正确）。
- **反证**：用软件同一组合距离公式（dHash9×8 full+center + pHash32 full+center + hist compareHist 加权）在 python 复现：2号位 crop 对 **046(轮滑鞋)=276（最近）**，对 **045(生命之花)=317**，055=409/051=400/044=337。→ **046 本应 distance 最小当选，识别器却把它错判为 045/空 = 识别器实现有 bug**（非图案难分，印证用户判断）。
- 待办：在 **C# 识别器内部**取 2号位 045/046 真实 distance（验证环境须一致）；若 C# 确证 046<045 却被拒 → 定位识别器实现 bug（滑窗/bestSlide/compareHist/DCT）。工具图 tools/aglaia_slot1_true.png(及_x6)。

## 轮滑鞋(046)根因确凿（2026-08-20，用户指认左移10px + 距离复现）
- 用户指认：fixedSlot1=(974,616,45,48) 的 crop 偏右约10px，**左移10px(→964)盖准轮滑鞋**。用软件识别器相同组合距离公式：
  - 2号位 crop 左移10px：**046(轮滑鞋)=166（最近）**，045(生命之花)=287，044=295，051=372，055=416。→ 左移后 046 正确胜出（原错认045）。
- **三槽统一左移10px验证（2559×1439 帧）**：1号055原位358→左移279；2号(046)317→166；3号051原位286→左移168。**三槽左移10px全部更准 = 固定槽 x 坐标在2K分辨率下整体偏右约10px（系统性偏移，非2号位特殊）**。
- 待用户定修复（未动代码）：
  - A（治标，快速）：fixedSlots 三槽 x 左移10px（当前2K帧有效；1080p会如何需确认）。
  - B（治本，推荐）：查为何 fixedSlots `/1920→ToPixels(帧宽)` 等比缩放导致2K下偏右10px——可能游戏2K下装备带相对角色卡的位置不是纯等比；评估是否应在缩放中统一修正。回归用 1/3号+2号 三槽+其它帧。
- 中间教训：千问视觉给像素坐标不可靠（给了 x=200-359 完全不对），像素级定位仍须用户拍板；我乱裁多次浪费时间，坐标判断绝不自己定。

## 装备槽"偏移量"全局面调查（2026-08-20，6帧×前台/后台×3段槽，前景质心法）
- 偏移**不是全局常数**：
  - **132307**（阿格莱雅帧）：前台slot0 三槽偏移 +12/+10/+12（图标偏右≈10-12px，=需左移10px），后台slot0 +22。
  - **125924**：前台slot0 +28/-17/-16，后台slot1 +23/-15（偏移较大且方向混杂）。
  - **130104/130112/130123/132328**：前台后台几乎都 -1~-2px（基本准）。
- **结论**：装备槽在 2K 帧下**逐帧/逐角色偏移量不同**。全局"左移10px"会修好 132307 却弄坏 130104 等帧——**这正是"反复改坏坐标"的根源**。
- **代码坐标多套不一致**（治本关键）：
  - 固定路 `RecognizeEquipmentSlots`：Front{0.03,0.39,0.72} 槽宽0.26；Back{0.03,0.34,0.68}。
  - `BuildEquipmentSlotsByCount`（段路自适应）：Front{0.03,0.39,0.75} 槽宽0.33；Back{0.03,0.34,0.68}；**1件=卡中心**（注释：用户2026-08-10定稿，原套0.39因卡宽不同偏14px）。
  - `BuildEquipmentSlotsFromSegments`：非蓝段自适应。
- **用户提供布局规则**：一件装备的坐标 = 三件装备中间件坐标（卡中心/中间槽），代码 ByCount 已按卡中心实现（1件）；固定路未确认一致。
- 待用户决策（未改）：统一坐标来源 + 逐角色/逐帧自适应，而非固定分数偏移。工具 tools/eq_offset_survey.py。

## ★ 重大认知纠正（2026-08-20 晚，用户强烈不满后）
### 我今晚犯的大错
- 用户 8/19 用**真实实机截图 clipboard-2026-* **逐张归档装备/识别错误（docs/REVIEW_20260819_错误归档根因方案.md），明确 A4/A5 = 后台藿藿生命之花、千冶刃装备漏识别是**待修遗留**。这些才是正确的修复素材。
- 我 8/20 晚**没用用户的真实截图**，而是拿 `tests/.../Fixtures/phase2-2026-07-28/{125924,130104,130112,130123,132307,132328}.png` 这批 fixture 反复裁图/算距离/改坐标。**千问确认这批图是《崩坏：星穹铁道》画面，不是 CWT 备战页** → 我在错误图上空转一整晚，产出全无意义，还让用户核对错误位置的图。这是我今晚最大的错误。
- 教训：**修复必须基于用户真实实机图（clipboard-*/真实回放），先确认素材是 CWT 备战页再动手坐标**；不能拿来源不明的 fixture 直接调坐标。素材路径需找齐（clipboard-2026 图在何处待确认）。

### 正确素材/根因（来自 8/19 文档，非我空转所得）
- A4：后台藿藿「生命之花」装备漏识别。A5：后台藿藿/千冶刃装备带漏（能量激活时）。皆"后台装备带识别弱"。
- 拆装扳手数量(B1)：特殊物品栏识别只取1个，需堆叠/数量识别。冶金炉(B2)：模板/路径缺失。
- 装备识别历史：855fix 已做三处根因(68fa259)+种类判别(978dd21)+pHash组合距离(8ce5123)——遗留后台装备带识别弱。

## 装备识别 8/20-21 最新真实状态（用户多次暴怒后，如实记录，勿再重复踩坑）
### 用户拍板的确定事实
1. 用户确认：**4 张装备红框图（tools/eq_box_back{6,7}_mode{2,3}.png）的底图是正确的**（备战页正确底图=phase2-2026-07-28/130104 与 125924）。用户说"装备图不是已经弹了正确的"。
2. 用户判断这 4 张装备图：**y 轴对，x 轴偏太严重**，肉眼看不出具体偏几像素，用户不想再重复装备坐标推算规则。
3. user 要求：用**相同底图**弹"角色卡框"图来核对卡框坐标（因为装备坐标是角色卡坐标+规则推出来的，卡框对了才能推装备）。
4. **我反复弹成"战斗页"被用户痛斥**——但经 python 比对，cardbox_6slot_130104.png 与 eq_box_back6_mode3.png 是**同一底图**（130104），仅叠加框不同。用户仍喊战斗页，说明**用户对这些 fixture 是否真为备战页存在分歧，或底图类别判断存疑**。不要再自行判断页面类别，以用户看到的画面为准。

### 已确认的技术事实（非空转所得，可信）
- `PreparationCharacterSlots1920`(前台4格) 与 `BackCharacterSlots1920(n)`(后台) 是 1920×1080 像素参考坐标，代码统一用 X*W/1920、Y*H/1080 缩放到实际帧(2559×1439)。
- 固定装备槽：Front{0.03,0.39,0.72}/Back{0.03,0.34,0.68}、槽宽0.26、y=卡底+1.08卡高；ByCount 另一套 Front{0.03,0.39,0.75}/槽宽0.33；2件=卡中心±21px槽宽0.30；1件=卡中心。
- 132307 阿格莱雅 2 号位(轮滑鞋046)：软件 crop(fixedSlot1 px=974,616,45,48)，dHash 候选=045 但 conf<0.90 → Empty。用软件组合距离公式 python 复现：左移10px后 046=166(最近)，045=287 —— 识别器能分(046更近)，但 absolute>160 可靠线 → 拒不输出。用户指认"左移10px盖准轮滑鞋"。

### 待用户给方向（未改任何代码）
- 修正装备/角色卡 X 偏移：需要用户在正确备战页图上指出每格 X 偏移方向+量；或用软件卡框+规则自动推。
- 不要再自作主张改坐标、不要再干弹图(战斗页)惹用户暴怒。用户明确"别搞了，先写 Handoff"。
