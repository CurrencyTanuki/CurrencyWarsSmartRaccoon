# 下一班 AI 工作令（2026-09-04 09:4x 交班）——货币战争智能狸·决策层监督与开发

> 你接手的是夜间连续开发班次。上一班 AI 交班时状态：**决策层引擎（GrailDecisionEngine，1.2.50）可能仍在自主跑局**；软件经计划任务 CWSmartRaccoonCmdTest 运行；游戏开着。
> 你的职责：**监督决策层自主跑局 + 修缺陷 + 持续优化**。用户整晚/全天授权自主工作，卡点积累自救，不中途询问（重大破坏性操作除外——重开一局已获长期授权，不算重大）。

## 零、必读三件套（按序）

1. `HANDOFF_SESSION_20260902.md` 顶部节 **「★★★ 夜班交接与下一班工作令（2026-09-04 09:4x，最高优先阅读）★★★」**——当前状态/完成清单/已知缺陷 C 节/任务计划 D 节/监督方法论 E 节。
2. `rule.md`——全部规则+坑账本（重点：坑 38/38a/39/40/41，全部是昨夜实战教训）。
3. `docs/DECISION_LAYER_BLUEPRINT_20260903.md`——决策层总纲（S1~S8 + X1~X14 情况处置 + 防错清单）。

辅助：`docs/GRAIL_COMMAND_SET_v43_final.md`（v4.3.2 增补已含 A15/禁选280/敌情取消/红档锚点/看门狗）；决策树 `docs/DECISION_TREE_1-3三星五费_定稿版_20260829.mmd`（09-03/04 注已入树）。

## 一、接手验证（10 分钟）

1. 文件通道：软件 exe 同目录写 `指令测试-command.txt`（一行一指令），读 `指令测试-result.txt` 等「⇒」终态。写文件=立即发出。
2. 发 `STATUS` → 确认游戏窗口与软件；发 `START` → 确认识别会话；发 `I1` 看页面（帧龄>15s=陈旧，等重试）。
3. 发 `DECIDE 开始` 若决策层未运行；`DECIDE 停止` 可停；运行期间其他游戏指令会被拒绝（防互抢，1.2.49 起）。
4. **监督节奏铁律：每 ≤2 分钟读一次回执尾并深度思考**；跨源交叉验证（截屏+分类器诊断测试 tests/CurrentFrameClassifierDiagnosisTests.cs，设 CW_DIAG_FRAME 跑单测）；日志说正常≠正常，识别错/决策错在日志里都是"正常"。
5. 反证即停：回执与盘面矛盾（金币对不上/卡消失/Fail 但有进展）→ 立即停止序列，先定位。

## 二、监督决策层跑局

- 引擎自主循环：M8 刷开局（守卫拦截续局/导航失败会 A9 清场重试）→ 命中 067/019 进 1-1 → 1-1/1-2 流程 → 1-3 策略(M7) → 运营循环（M5 圣杯/部署/祈愿 M3/卖冗余）→ 终局判定 → R3 自动弃局重开。
- **你只监督不代打**：每 ≤2 分钟核对回执与截图；引擎卡点（重试预算/守卫/Unknown 页）先让它自愈一轮，无效再介入。
- 常见卡点对照 handoff「C. 已知缺陷与疑点」：M5 默认部署互换（未修）、远坂凛卡识别误读（未修）、score_popup 弹窗（未修，可先 A15/人工 Esc）。
- GPU 崩溃恢复：重启游戏→等 1 分钟→主界面→开始游戏→继续挑战→STOP/START 重连识别→I10 对账防回档。

## 三、修缺陷流水线

编码 → 子代理对抗审查（每批硬性一轮，夜间也不许跳——上 night 班跳过审查被用户定性为事故，rule.md 坑40）→ 修复 → 构建 0 警 0 错 + Grail 短套件全绿 → 发布（artifacts/deploy_stable_*.ps1，失败必须中止串链重跑）→ DECIDE 重启 → 继续监督。

## 四、当前已知待办（按优先级，详见 handoff「D. 完整任务计划」）

1. **P0**：监督跑局 + run-summary JSONL（引擎每局落一行摘要）。
2. **P1**：M5 默认部署互换修复（改真实空槽）；血量=0 自动弃局；score_popup 锚点补全；防错清单剩余单测。
3. **P2**：全员模式路径；组合根接线；节点锚点持久化；8 条历史 bug 实机回归。
4. **缓办**：装备识别专修（非星徽）；捕获降载；I10 人口口径。

## 五、红线（违反=事故）

软件仅在用户授权范围内行动（跑局+修复+发布测试台已授权）；正式留档包/冻结备份不动；未验证不下结论；同类错误两次=停工记录根因换路线（夜间无人，天亮汇报）；修改完成同一回合写入 handoff；部署失败必须中止串链重跑。
