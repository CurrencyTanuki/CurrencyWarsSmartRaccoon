# 「货币战争智能狸」指令集 v4.3 —— 修改建议审计报告（终版）

- **日期**：2026-09-01
- **性质**：仅给出修改建议，不重述原缺陷原文（原审计见 `AUDIT_指令集v43_20260901.md`）
- **范围**：仍只审 `GRAIL_COMMAND_SET_v43_final.md` 文档，不动 `src/` 代码
- **已吸收的用户裁定**：
  1. **血线裁定**：血 = 88 不可点、血 = 89 可点 → 拒绝条件统一为 `血 < 89`（血 ≤ 88 拒、血 ≥ 89 选）。
  2. **P0-2 裁定**：奇迹代偿优先，但血 < 89 时跳过它、转次高优先级（无限之釜）；判定为"好修"。
  3. **P1-3 裁定**：顶层编排（谁来调用）属实现层/下一步开发内容；软件分识别层·决策层·操作层，调用编排由三层架构承担，指令集本身不必补 caller。

---

## 一、P0 阻断项（两条均已裁定，直接落地修改）

### P0-1 血量红线统一为 `血 < 89`

**修改点（指令集内部）：**
- 第二部分"思想一"L28 的 `血 < 88` → 改为 **`血 < 89`**，并补句"血 = 88 亦在拒选区（保守侧）"。
- M3 优先级表 L175 维持 `血 < 89 绝不选`（本就正确，无需改）。
- M3 表补一行：**`血量未知 / 识别失败 → 绝不选，选另一侧`**（消除"识别失败"无落点的缺口，原思想一 L28 要求的防御分支在此接线）。
- 第三部分机制 L83 的"仅血 ≥ 89"维持。

**联动修订（必须同步，否则与权威源再裂口）：**
- 决策树定稿版 `F13 / F15 / F15a / L2` 当前口径为"判定线 > 88、危险线 < 88、血 = 88 悬空"。用户裁定"88 不可点"与该口径冲突，需把决策树改为 **`血 < 89` 危险、`血 ≥ 89` 可选**，消除 88 悬空。（注：决策树为用户设定的权威源，本次由用户直接裁定覆盖其原 88 边界，故决策树须回写。）

### P0-2 M3 裁决规则统一为"优先级排序 + 各自闸门"

**修改点（指令集内部）：**
- 把"M3 优先级表（按序匹配）"与"M3 补充规则（双侧同现取优先级高者）"**合并为同一套算法语言**，删除任何暗示"表顺序优先"或"补充规则优先"的绝对表述：
  > 双侧/多侧关键试炼同现时，**按优先级从高到低遍历；每个试炼须先通过自身条件闸门（奇迹代偿闸门 = 血 ≥ 89）才能被选中；第一个过闸门的即执行；全部不过闸门则走 M3 第 8 条弃局重刷。**
- M3 表第 2 条原"奇迹代偿血 < 89 → 绝不选 → **选另一侧**" → 改为"奇迹代偿血 < 89 → **跳过（不中断，继续按优先级选下一可行项）**"，与补充规则统一。
- 奇迹代偿序位维持最高（序 1/2）；无限之釜序 5/6。组合「奇迹代偿@血<89 + 无限之釜@单人」→ 跳过奇迹代偿 → 选中无限之釜 → 收工 ✓（不再卡死）。

---

## 二、P1 严重项

### P1-3 重分类（按用户裁定：设计如此，留待实现层）
- 不再作为文档缺陷。指令集定位为"零件清单"，调用编排由三层架构（识别层→决策层→操作层）在实现层承担。
- **可选建议**（非阻断）：在文档末尾加一节"三层映射说明"，一句话点明 I 类归识别层、M/A 类归操作层、决策层状态机（第五部分）负责编排与终局判定，便于实现对齐。M2/M6/M8 的挂载点由该层解决，无需在指令集内逐条挂 caller。

### P1-4 硬规则「购买预设禁用 + 银河学者启用」补承载
- 新增指令或在 M8 内部显式写明并标注底层组件：`A13 购买预设开关`（1-1/1-2 禁用三仙舟+2DOT 预设）、`A14 策略开关`（仅 1-1 启用银河学者/猫猫糕）。
- M8 内部流程（L166）补这两步，对应决策树 N1①②。

### P1-5 I5/I6 强制刷新时机
- 规定刷新点："每次检测到祈愿弹框上沿 → 必发 `I5`（读当前血量）后再发 `M3`"；"每 N 次商店操作 / 每次卖人后 → 发 `I6`"。
- 目的：给思想五的 last-known 缓存接补给入口，避免 88/89 边界用陈旧血量误判。

### P1-6 M8 参数补全
- M8 参数列（"—"）增 `白名单`（未购买命杯成员 + 昔涟），或明确"M8 内部复用 M5，白名单由决策层注入透传"。避免决策逻辑漏进宏。

### P1-7 星徽装配指令流与时序
- 显式序列：`I7 → I3 → A4 → A1`。
- 单独规定"019 命运圣杯邀请路线：星徽开局即得，装配上场须在 M8 阶段（进 1-1 之前）触发"，不要挂在 1-3 运营循环内。

### P1-8 M5 终止条件"刷新无效"改为可判定
- 改为：`金币 < 刷新价 → 停止`，或 `连续 K 轮货架角色签名不变 → 停止并上报`。
- 历史 bug「刷新不生效空转」必须证明不复发，此条是最可能复现路径，必须闭环。

### P1-9 宏内决策动作上收
- M6 的"弃局重开"、M3 第 5 条"收工停机"、M3 第 8 条"弃局重刷"改为：**宏只回事实**（选中试炼名 / 命中环境 ID / 四次是否用尽）；弃局与收工统一上收决策层 `GrailFinalJudge`。消除双重真值源，且防止 M6 永不返回。

### P1-10 067「19 节点内不能上场」补约束
- 第三部分 L97 补该约束；A1/A5 标注"067 附赠 5 费在 19 节点内不可上场、不激活羁绊"。

---

## 三、P2 应修项

- **P2-11 A10 参数「槽位号」** → 改为语义参数（策略名 / 优先级序号），槽位号由操作层解析（守思想二）。
- **P2-12 M4 返回「几号位」** → 删除，或标注"仅诊断，决策层不得使用"，避免诱导依赖过程数据。
- **P2-13 M4 依赖 LettersObtained 无防错** → M3 返回实际奖励本数；或扩展 `I7` 为"读物品栏（星徽 + 聘用书本数）"，让状态机核对实本数，防历史 #12 `LettersObtained 恒+2` 复发。
- **P2-14 M3 第 3 条排除四费聘用书** → 写明"精确匹配五费，显式排除四费聘用书"（继承历史 #12 修复，当前未继承）。
- **P2-15 A6 契约补全** → 增金币参数；返回改为"实际买成次数 + 当前人口"，覆盖诅咒价（+1/次 → 2 次需 10 金）下只买成 1 次的表达。
- **P2-16 节点锚点** → 状态机显式维护"当前节点"变量，第五部分规定推进条件与校验点，使"不跨过 1-3""1-1/1-2 禁售"有可验证锚点（应对 M8 异常提前返回）。
- **P2-17 I8 与 M3 职责重叠** → 明确 `I8` 仅供 M3 内部与诊断，决策层只发 `M3`，消除优先级表两处实现。

---

## 四、P3 建议项

- **P3-18 进 1-3 初始化序列** → 补一节：先开晶矿（M2）→ 先卖非命杯凑钱 → 统一上场命杯成员（对应第三部分 L104 / 决策树 N13）。
- **P3-19 A2/A3 阶段约束** → 标注"仅 1-3 可用"（1-1/1-2 禁售）。
- **P3-20 M7 三连刷兜底** → 定义终止（选最左继续 / 上报由决策层处置），防空转。
- **P3-21 血量归零→弃局** → 第五部分事件驱动规则补"血量归零 → 发 `A9`"，对应决策树 R3。

---

## 五、遗留待确认项（原"需用户拍板"更新后）

1. **决策树回写**：用户裁定"88 不可点"与决策树当前 F15/F15a「88 安全」冲突，需同步修订决策树（见 P0-1 联动修订）。—— 已裁定，仅待执行回写。
2. **067「19 节点内不能上场」**（P1-10）是否仍为当前版本游戏行为？若已变需同步改决策树 R67。 —— **仍待你确认**。
3. **无触发者 9 条中的 I10「全量识别」/ A8「概览确认/下一页」**：M2/M6/M8 由三层架构解决；这两条保留还是删除？ —— **仍待你定**（建议保留并标注用途，或删除）。

---

## 六、修改后预期状态

| 项 | 裁定/处置 |
|---|---|
| P0-1 血量红线 | 已裁定，统一 `血 < 89`，待回写决策树 |
| P0-2 裁决互斥 | 已裁定，合并为"优先级+闸门"算法 |
| P1-3 装配图 | 设计如此，留实现层（三层架构） |
| P1-4~P1-10 | 给具体修改建议，待落地 |
| P2-11~P2-17 | 给具体修改建议，待落地 |
| P3-18~P3-21 | 给具体修改建议，待落地 |

> 本报告未修改任何源文件，仅新增本文件。所有结论来自文档交叉比对与用户裁定，未对 `src/` 代码下结论。
