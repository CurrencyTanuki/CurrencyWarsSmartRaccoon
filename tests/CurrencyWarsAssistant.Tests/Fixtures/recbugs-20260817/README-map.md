# 关键帧 ↔ 问题映射表（2026-08-18 建立）

> 来源 run：`C:\Users\zzz81\AppData\Local\CurrencyWarsSmartRaccoon\runs\run-20260817-193442`（用户实机，问题全部复现）
> 用途：修复 A/B/C 后用这些真实帧回放，验证问题是否修复（真实帧回放测试，非模拟帧）。
> 注意：**这些帧不含可暴露的 UID 敏感信息**；若将来有含 UID 帧需遮罩后再入库。
> 每帧都附了该帧 analysis 的"应有真值 / 软件当前错误"以作断言。

## 关键帧清单

| 帧文件 | 对应问题 | 软件当前结果（错） | 应有真值 |
|---|---|---|---|
| `20260817-114638890.png` | **狸猫→佩佩**（B/增量固化 + C2 特殊单位漂移） | board/lineup 识别成 `special_unit_tanuki_112`（对）但 **special 区识别成 `special_unit_peipei`**；后续增量帧复用 peipei 固化覆盖 | 狸猫应是 `special_unit_tanuki_112`，不应出现 peipei |
| `20260817-115317510.png` | **1-7 结算漏霍霍 + 头像 unknown**（C1） | 结算伤害 rank1 `unknown-character-69`（霍霍=c69），头像 conf 0.48，候选仅误填大黑塔22；rank3/4/6/7 也 unknown | 霍霍(c69) 头像应唯一识别，不应 unknown/错填 |
| `20260817-113811949.png` | **后台/备战席区域错乱 + 装备**（C3/C4） | back 区含 `unknown-formation-unit-35`；bench 区出现 c55×2+c10；装备含 eq_043 等 | back 与 bench 角色应归对区域；后台角色不应进备战席 |
| `20260817-115536659.png` | **冶金炉丢失**（C4） | `special_item_004`(冶金炉) 仅在识别内部出现 1 次，落盘/历史丢失 | 冶金炉特殊装备应被保存并显示 |

## 非截图类问题（性能）

| 问题 | 验证方式 |
|---|---|
| 单帧识别 19-72s / PP-OCR 预热失败 | 用正式批测入口 `CURRENCY_WARS_PHASE2_TIMING=1 App --phase2-batch-test` 测单帧耗时，目标回 1.5-2s；启动无"PP-OCR 预热失败"报错 |

## 用法
- 修复后用这些帧跑 analyzer 回放，断言每帧输出 == 应有真值列，且不再出现错误结果。
- 有向回归：修某问题后，其它问题帧仍必须保持正确（防把好的改坏）。
