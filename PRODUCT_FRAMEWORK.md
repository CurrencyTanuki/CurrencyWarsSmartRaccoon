# “币站开局小助手”项目定位与开发框架

当前阶段使用“币站开局小助手”作为项目名称。待局势识别、评估与个性化建议等高级功能成熟后，项目更名为“货币战争智能狸”。

## 一、项目定位

这是一个面向《崩坏：星穹铁道》货币战争玩法的源码公开、免费桌面辅助工具。

项目代码、功能说明和使用方法公开，任何人都可以免费下载、用于非商业目的、研究和参与贡献。项目不提供付费功能、会员功能或封闭服务。

工具通过以下方式工作：

- 获取游戏窗口画面；
- 通过图像识别和 OCR 理解界面内容；
- 根据公开规则及攻略数据进行判断；
- 显示识别结果、攻略和操作建议；
- 在用户启用自动操作后，通过模拟鼠标完成指定操作。

项目不读取或修改游戏内存，不注入游戏进程，不修改游戏文件，也不尝试绕过验证或反作弊机制。

## 二、功能范围

### 1. 开局辅助

- 识别开局投资策略；
- 识别敌人正面或负面词条；
- 识别其他影响开局的选项；
- 设置必须包含、允许出现和必须排除的条件；
- 支持开局加权评分；
- 自动判断保留或重新开始；
- 模拟鼠标完成重开流程；
- 达到目标后停止并提醒；
- 设置最大次数、最长时间和紧急停止。

### 2. 攻略资料

- 投资策略资料；
- 敌人词条资料；
- 角色、羁绊和阵容资料；
- 经济运营与阶段策略；
- 搜索、筛选和交叉引用；
- 根据当前画面自动显示相关攻略；
- 标记资料所适用的游戏版本。

### 3. 局势识别

- 识别回合和当前阶段；
- 识别金币等资源；
- 识别已有角色、等级和阵容；
- 识别当前羁绊和成型情况；
- 识别商店或当前可选内容；
- 识别敌人信息及特殊词条；
- 将识别结果整理为统一的对局快照。

### 4. 局势评估

- 评估当前经济状况；
- 评估即时战力；
- 评估阵容成型度；
- 评估后续发展空间；
- 标记当前主要风险；
- 展示缺失或识别不确定的信息。

### 5. 下一步建议

- 生成当前可执行的候选操作；
- 比较不同操作的收益、成本和风险；
- 给出具有优先级的建议；
- 展示建议依据；
- 支持不同策略倾向；
- 允许用户确认后执行建议。

### 6. 自动操作

- 根据当前页面定位按钮；
- 将语义动作转换为鼠标操作；
- 点击前验证页面和目标；
- 点击后验证操作结果；
- 界面未知或识别不确定时暂停；
- 对出售、放弃、消耗关键资源等操作要求人工确认；
- 支持全局紧急停止。

## 三、运行模式

项目提供三个逐级开放的运行模式：

### 观察模式

只识别并展示游戏状态，不执行任何鼠标操作。

### 辅助模式

识别局势并给出建议，由用户确认后执行。

### 自动模式

只在用户预先配置的范围内，自动执行重复、低风险且结果可验证的操作。

首个可用版本先完成观察模式，再加入自动刷开局。

## 四、整体开发框架

### 1. 画面采集层

负责稳定获取游戏窗口画面：

- 定位和锁定游戏窗口；
- 获取完整画面或指定区域；
- 处理窗口移动、缩放和失焦；
- 统一不同分辨率下的坐标；
- 控制截图频率。

### 2. 图像识别层

负责从画面提取可识别内容：

- 判断当前界面；
- 定位按钮、卡片、图标和文字区域；
- 使用模板匹配识别稳定图标；
- 使用 OCR 识别名称、说明和数值；
- 输出识别位置与置信度；
- 保存低置信度截图供后续改进。

### 3. 状态管理层

负责将零散的识别结果整理为当前游戏状态：

- 合并连续多帧结果；
- 过滤动画和临时遮挡；
- 记录当前页面；
- 记录开局信息；
- 记录完整对局快照；
- 标记未知、缺失和互相冲突的信息；
- 保存近期状态和动作历史。

### 4. 数据与攻略层

负责保存可由社区共同维护的游戏资料：

- 投资策略；
- 敌人词条；
- 角色、羁绊和阵容；
- 版本化攻略；
- 名称别名和识别图标；
- 策略评分及搭配关系；
- 数据格式校验和版本迁移。

事实数据和主观策略评价应当分开保存，方便社区修正和替换。

### 5. 判断与建议层

负责根据当前状态产生结论：

- 执行开局保留、排除和评分规则；
- 计算经济、战力、成型度和风险；
- 从攻略库中检索相关内容；
- 生成合法的候选动作；
- 对候选动作排序；
- 输出建议及可解释的判断依据。

建议引擎必须区分当前阵容、过渡阵容、目标阵容和本局特殊目标。阵容之间可能完全不兼容、部分兼容或只能单向转型，不能使用单一强度榜直接生成建议。稳定通关、追三星五费和追求核爆数字等目标分别使用不同的评价权重。

每条由外部攻略转化的规则保留作者、原始链接、适用版本和具体依据。建议界面底部显示参考资料，便于玩家打开原文或视频深入了解。

### 6. 操作执行层

负责安全地执行鼠标操作：

- 检查动作的前置页面；
- 定位需要操作的界面元素；
- 转换窗口坐标和屏幕坐标；
- 执行单击、移动和滚动；
- 等待并验证操作结果；
- 失败时重试、恢复或暂停；
- 拒绝在未知页面盲目点击。

### 7. 桌面界面层

负责展示信息和接收用户配置：

- 选择游戏窗口；
- 启动、暂停和紧急停止；
- 设置开局条件；
- 显示当前截图和识别框；
- 显示当前状态及置信度；
- 显示攻略、评估和下一步建议；
- 管理观察、辅助和自动模式；
- 查看操作历史和错误信息。

### 8. 日志与测试层

负责保证项目可维护和可验证：

- 保存识别输入和结构化结果；
- 保存决策理由及操作结果；
- 建立脱离游戏运行的截图测试集；
- 测试不同分辨率和显示缩放；
- 回放完整操作流程；
- 统计识别准确率和失败类型；
- 为游戏版本更新提供回归测试。

## 五、系统数据流

```text
游戏窗口
  → 画面采集
  → 图像识别
  → 状态管理
  → 数据查询与规则判断
  → 显示识别结果、攻略或建议
  → 用户授权检查
  → 鼠标操作
  → 重新截图并验证结果
```

所有自动操作都必须形成闭环：识别当前状态、执行一个动作、重新识别并确认结果，然后才能执行下一个动作。

## 六、开发阶段

### 阶段 1：开局识别验证

- 选择并捕获游戏窗口；
- 识别开局页面；
- 识别投资策略和敌人词条；
- 展示结果、位置和置信度；
- 建立离线截图测试集；
- 暂不自动点击。

### 阶段 2：自动刷开局

- 配置保留、排除和评分规则；
- 实现重开流程状态机；
- 模拟鼠标执行重开；
- 验证每一步操作结果；
- 加入次数限制、运行时限和紧急停止；
- 命中目标后停止并提醒。

### 阶段 3：攻略资料库

- 建立公开、版本化的数据格式；
- 导入投资策略、词条、角色和阵容资料；
- 支持搜索和筛选；
- 根据当前识别内容显示相关攻略；
- 提供数据校验和社区贡献方式。

### 阶段 4：完整局势识别

- 识别资源、阵容、商店、羁绊和敌方信息；
- 生成统一对局快照；
- 支持截图回放和人工纠错；
- 扩充识别准确率测试集。

### 阶段 5：局势评估与建议

- 实现经济、战力、成型度和风险评估；
- 生成下一步候选操作；
- 对建议进行排序和解释；
- 首先只展示建议；
- 验证稳定后开放用户确认执行。

### 阶段 6：有限自动执行

- 只开放高置信度、低风险操作；
- 为不同操作设置权限等级；
- 对关键操作强制人工确认；
- 完善异常恢复、日志和回归测试。

## 七、建议的代码结构

```text
app/
  perception/       # 窗口捕获、图像匹配、OCR、页面识别
  state/            # 状态模型、连续帧稳定、历史记录
  knowledge/        # 攻略和游戏资料读取
  decision/         # 开局规则、局势评估、建议生成
  action/           # 鼠标操作、安全检查、流程状态机
  ui/               # 桌面界面
  telemetry/        # 日志、截图证据、错误报告
data/
  strategies/       # 投资策略
  modifiers/        # 敌人词条
  guides/           # 攻略
  templates/        # 图像识别模板
tests/
  screenshots/      # 脱离游戏运行的测试截图
  perception/
  decision/
  workflows/
docs/
```

## 八、公开项目设置

正式公开前应补齐：

- `README.md`：项目说明、安装方法和使用方法；
- `LICENSE`：明确源码授权及非商业使用限制；
- `CONTRIBUTING.md`：代码、攻略和截图数据的贡献方法；
- `CODE_OF_CONDUCT.md`：社区行为规范；
- `SECURITY.md`：安全问题报告方式；
- Issue 和 Pull Request 模板；
- 自动测试和发布流程；
- 游戏版本兼容说明；
- 非官方项目及与游戏官方无关联的声明。

GPL-3.0 允许商业使用和收费分发，因此不能实现“禁止任何人将代码用于商业用途”的要求。OSI 对开源软件的定义同样要求不得限制商业领域的使用。

如果“禁止商业用途”是不可更改的条件，应采用非商业源码公开许可证，例如 PolyForm Noncommercial 1.0.0。采用这类许可证后，项目可以公开源代码并供个人免费使用，但不应再宣称采用 OSI 定义下的“开源许可证”。

最终许可证应在首次公开发布前确定，不能同时声明“GPL-3.0”与“禁止商业用途”，因为两者在授权范围上互相冲突。

## 九、首页免费声明

软件首页、README 和发布页面应醒目显示：

> 本软件由项目官方免费提供，不收取软件购买费，也没有付费版或会员版。若您通过付费渠道获得本软件，请注意该渠道与本项目无关；建议联系商家申请退款，并从项目官方发布页免费获取。

为了避免产生错误的法律含义，应使用“非官方付费渠道”，而不是笼统称为“非正规渠道”。如果最终选择 GPL-3.0，第三方对软件副本收费并不当然违反许可证；如果最终选择非商业许可证，则可以进一步增加：

> 未经作者另行书面许可，禁止销售本软件，或将本软件及其代码用于商业用途。

## 十、开发原则

1. 所有官方功能免费、代码公开。
2. 游戏数据与程序代码分离，方便社区维护。
3. 先完成识别，再开放自动操作。
4. 不确定时停止，不进行猜测点击。
5. 每个建议都能展示主要判断依据。
6. 每个鼠标动作都必须验证结果。
7. 每个游戏版本更新后先运行回归测试。
8. 优先使用可离线运行的开源依赖。
9. 不强制用户上传截图或对局数据。
10. 项目名称、说明和界面明确标注为非官方社区工具。
