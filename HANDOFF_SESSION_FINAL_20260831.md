# 会话交接（2026-08-31 深夜）— 指令集 v4.3 定稿，待架构重构

> **给新上下文的 AI：本文档是你的工作起点。读完本文档 + docs/GRAIL_COMMAND_SET_v43_final.md 即可无缝接手。**
> 当前 Git HEAD：见 `git log --oneline -5`。工作树干净，全部成果已提交。

---

## 一、当前状态一句话

**指令集 v4.3 已定稿归档（`6724c2a`），架构重构尚未开始**——这是你接手后的第一件事。

## 二、本轮会话做了什么（时间线）

1. **决策树定稿**（`ac80f43`）：用户四轮拍板，定稿版在 `docs/DECISION_TREE_1-3三星五费_定稿版_20260829.mmd`
2. **纯逻辑核心**（`f6e986a`）：Grail* 五文件实现决策树，65→79 测试
3. **适配层+编排**（`11dc669`→`49254d3`）：组装器/执行器/监听器/整局循环/MainWindow 接线/N1 配置
4. **删除旧 FateGrail 决策树**（29 文件），录屏迁移为 GrailRollingRecorder
5. **三层审计 + 多轮修复**：找到并修复大量 bug（详见 docs/AUDIT_FINDINGS_20260830.md）
6. **实机三轮热修**：奇迹代偿必选（F14a 删除）、停止急停闸、性能优化、卖人坐标错系修复（CardRegion 是 warp 画布坐标不能当窗口坐标！）
7. **指令集 v4.3 定稿**（本次最终成果，见下）

## 三、指令集 v4.3（最终成果——你的重构蓝图）

**完整文档：`docs/GRAIL_COMMAND_SET_v43_final.md`（必读，含设计宪法/游戏机制/全部指令/优先级表/验证要求）**

摘要：
- **三层架构**：识别层（要数据）/ 决策层（状态机+发意图）/ 操作层（执行+返回已验证事实）
- **三层可任意跳转**，不固定流水线
- **指令参数全是语义**（角色名/策略优先级/模式），坐标写死在操作层
- **I 类 10 条**（识别）：页面/货架/备战席/上场/血/金/星徽/弹框/档位/全量
- **A 类 12 条**（操作）：部署/卖场上/卖备战席/装星徽/置最左/买经验/环境刷新/概览确认/弃局/选策略槽/三连刷/Esc恢复
- **M 类 8 条宏**（固定流程黑盒）：M1 开战/M2 晶矿/M3 弹框应答(优先级表写死)/M4 开聘用书(含按F)/M5 商店白名单购买/M6 投资环境选择/M7 投资策略选择/M8 通关至1-3
- **P0 前置流程**：每次开刷检测页面→自动导航到玩法主界面（独立于指令集）

## 四、决策公理（用户多轮拍板提炼，16 条，违反=返工）

完整版在 `docs/GRAIL_DECISION_AXIOMS_20260830.md`。最关键的几条：
1. **机会优先，条件后补**：奇迹代偿血≥89 必选（本体不前置）；聘用书到手=必能拿人；绝不因前置不满足放弃机会
2. 血<88 点奇迹代偿会被游戏拒绝卡死——唯一的安全红线例外
3. 成功只来自 无限之釜/奇迹代偿；全员=奇迹代偿+本体昔涟；无限之釜全员只是辅助
4. 成型（拖投影仪/追全员）是玩家爽点，软件绝不代劳
5. 人口状态机推算（3→4→买经验2次→5）；银狼=3费；同名只买一次
6. 停止=急停闸物理拒绝一切输入；不跨过 1-3
7. 星徽到手必须马上装（用户说了 5 遍）

## 五、你接手后的任务清单（按序）

1. **读** `docs/GRAIL_COMMAND_SET_v43_final.md`（重构蓝图）+ 本文档
2. **架构重构**（文档第五部分工作范围）：
   - GrailCommand/GrailCommandResult 统一类型
   - 指令分发器（决策层→I/A/M 分派→适配器）
   - 三层适配器（旧实现沉到适配器后面）
   - 决策层状态机重构（基于指令结果流转）
3. **已修 bug 的回归验证**（新架构必须证明不复发，清单在 AUDIT_FINDINGS 文档末尾）
4. **实机联调**（需用户配合，清单在 v4.3 文档第七部分）

## 六、已知未修问题（如实交底）

1. **opening 期弹框泵是盲的**（采集会话在命中后才启动）——已有 20s 监测+弃局兜底，但根治需改直接探测
2. **N13 仪式不存在**——用户已拍板删除"开局仪式"概念，改为事件驱动（买到就上场/拿到星徽就装配）
3. **场上卖出**：坐标已修（用识别层 CardRegion 实测卡位），但**尚未实机验证**
4. **放弃局目录**留在未完成列表（采集器无"取消即放弃"语义）
5. 12 项全量测试失败=既有环境敏感清单，非新回归
6. **A24 按 F 时机待确认**：用户说"开完书回主界面后按 F"——已在 M4 内实现，但按 F 的确切语义（每本一次还是全部开完一次）待用户最终确认

## 七、用户协作规则（铁律，违反=信任崩塌）

1. **未经明确指令不得交付**（不发布/不打包/不放桌面）
2. 交付用**未压缩文件夹**放桌面，不用 zip
3. **修复攒批**：一次性改完再交付测试，不挤牙膏
4. 每轮代码后必须：**子代理对抗式审查 + 跑测试**（单测+图集）
5. **文档≠代码现状**：以 Git+代码为准
6. **机制不查表不要断言**（历史教训：飞霄不是银河学者）
7. 事件组件外挂化，不焊死在固定流程位置
8. 用户远程时通过 ToDesk（设备码 794881908，服务需管理员启动）

## 八、关键文件速查

| 文件 | 内容 |
|---|---|
| `docs/GRAIL_COMMAND_SET_v43_final.md` | **指令集 v4.3 最终版+重构蓝图（本次核心交付）** |
| `HANDOFF_FOR_NEW_AI_20260830_FINAL.md` | 前棒合并交接（背景/机制/架构） |
| `HANDOFF_SESSION_FINAL_20260831.md` | **本文档** |
| `docs/DECISION_TREE_1-3三星五费_定稿版_20260829.mmd` | 决策树定稿（实现依据） |
| `docs/GRAIL_DECISION_AXIOMS_20260830.md` | 决策公理 16 条 |
| `docs/AUDIT_FINDINGS_20260830.md` | 审计发现与修复批次 |
| `docs/TRIALS_命运圣杯_米游社合集_提取.md` | 试炼机制资料（含三条更正） |
| `src/CurrencyWarsAssistant.Tasks/Grail*.cs` | 新实现（决策+编排） |
| `data/4.4/*.json` | 官方游戏数据（机制断言必查） |

## 九、测试基线

- Grail 单测：85/85 全绿
- 全量：1023/1041（12-13 败=既有环境敏感清单，含 PpOcr 预热类波动）
- 测试包：桌面 `CurrencyWarsAssistant-1.2.2-test`（注意：该包是坐标修复后、指令集重构前的版本）
