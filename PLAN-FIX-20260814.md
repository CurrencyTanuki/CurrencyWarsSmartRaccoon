# 货币战争助手 修复 + 性能优化详细计划（2026-08-14 新 AI 接手）

## 0. 工作布局（红线）

- **只读参照（绝对不改）**：`D:\Codex-2\work\cw843-shadow-20260811`（Codex 原始工作区，0.2.855 源码 + 全部 artifacts/backup/TRX）。
- **工作副本（所有修改在这里）**：`D:\CWAFix-20260814`（**已切换为 0.2.842 基线源码**——用户 2026-08-14 拍板；2396 文件，含 tools/docs；Git 仓库 main 分支 + 不可移动标签 `baseline-0.2.842`，首提交 `1ddd0c3`）。
- 工作区只读入口：`<ws>\cw843-shadow-20260811`（junction，只用于查阅，禁止写）。
- 运行证据：`C:\Users\zzz81\AppData\Local\CurrencyWarsSmartRaccoon\runs\`（65 runs，只读）。

## 1. 事实基线（已核实，含对交接文档的修正）

1. **修正**：checkpoint `finalizedNodeIds` 实际为 `['1-2','1-3','1-4']`，**不是空数组**（交接文档 NARRATIVE/MANIFEST 表述有误，可能是看错文件）。实机封存过 3 个普通战斗节点；**未封存的是 1-1（奖励关）与 1-6（迟到识别后 run 已暂停）**。
2. **1-1 消失根因（实锤）**：封存总闸门 `Phase2OperationalStateTracker.Observe`（386–505）要求 `hasPendingBattle`（至少一个战斗/结算帧）。1-1 是奖励关（秒杀、无 battle 帧，结算页被归到 1-2），四条 exit 全不满足：
   - ①稳定结算：`IsSettlementSummaryReady`（1957–1960）要求 `SettlementGoldReward.Status==Known`，但该字段已按用户 2026-08-06 拍板废弃（`Phase2OperationalScreenshotAnalyzer.ReadSettlementGoldAsync` 1418–1429 恒返回 Unknown）→ 条件①被永久杀死，普通节点也只能靠②④；
   - ②结算预算耗尽：需 `_bestSettlementFrame`，奖励关没有；
   - ③非战斗退出：`RequiredPageConfirmationFrames=2`（80 行）+ 排除 overlay 页（725–727），奖励关后弹 reward_shop 恰好是 overlay，且需要非战斗页连续 2 帧；
   - ④结算后继备战：需 `_bestSettlementFrame`，奖励关没有。
   - **结构性缺口**：没有"节点推进（备战 N → 备战 N+1）即封存 N"的兜底路径（NARRATIVE P1 建议的 `BoundaryConfirmed` 状态不存在）。
   - 奖励关硬规则（`FinalizeBattle` 2391–2405：1-1/1-2/1-8/2-6/3-6 血量Δ+2、完美通关）只在 exit 判定通过后才执行——对奖励关永远执行不到（鸡生蛋）。
3. **性能根因（结构性串行，实锤）**：100ms 捕获 → **单 worker** 识别循环（全量帧=模板分类 0.3–0.5s + situation OCR + 数十区域字段 OCR，1.5–3s/帧；关键帧再补 3 前驱全量）→ **单消费者**收集循环（tracker.Observe 每帧 JSON+SHA256 指纹、投影锁内重建、PNG/JSON 写盘全部 await）→ output channel(16,Wait) 反向阻塞。OCR 不可取消（PP-OCR ONNX 20s 超时后仅 MarkAbandoned，后台继续占 CPU）。截图落盘慢=排队，不是编码慢。0.2.855 的 trace 裁剪/出队跳帧只减诊断量，未解决吞吐。
4. **23 项失败分三组**（task-3 调查）：
   - A 组 10 项：确认帧数契约冲突（生产 2026-08-11 用户拍板 2→1 帧，旧测试断言 2 帧语义）；
   - B 组 5 项：交接包缺 `tools/`（Invoke-EquipmentDataPipeline.ps1）与 `docs/`（DATA_IMPORT_4.4.md、SCREEN_FLOW_1920x1080.md）——可从桌面 `CurrencyWarsCodex-Handoff-20260811.zip` 补；
   - C 组 8 项：真实帧识别契约漂移（fixture 齐全），需实跑定责。

## 2. 分阶段计划

> **2026-08-14 补充：吸收前任 AI 总结报告的工程方法论。** 其最终结论"这次不是缺少一个更聪明的补丁，而是缺少一套能阻止错误补丁进入软件的工程系统"成立。以下计划已据此重排：**先建工程系统（Git+基线+回放+门禁），再修 bug，再优化性能**。

### 工程系统先行（贯穿所有阶段，最高优先）

**E1 恢复真正基线**：**基线 = 0.2.842（用户拍板）** → 已在工作副本建真实 Git 仓库 + 不可移动标签 `baseline-0.2.842` → 源码/配置/测试/EXE 一一对应 → 后续所有修改必须是可独立撤销的提交（一次提交=一个单一根因补丁）。0.2.842→0.2.855 之间的改动（性能优化+封存修补）按 E5 门禁逐提交选择性搬回。

**E2 整局回放器先于生产代码改动**：完整回放链 `备战变化→战前最后阵容→战斗→结算→奖励→下一节点→封存→重启恢复`；每局需人工标注真值（节点清单、每节点战前最后阵容/星级、金币/血量/人口/伤害、装备/物品栏/特殊物品、投资环境/策略/敌方阵营、字段应何时出现在 UI、哪些节点不得凭空生成）。**没有能稳定复现问题的红色回放测试，不允许改生产代码**（前任总结的核心教训）。

**E3 性能测试必须按真实时间持续喂帧**：不允许等上一帧处理完再喂下一帧（否则最严重的排队不会出现）。必报指标：capture→UI P50/P95/P99、capture→node finalized、UI 显示内容相对游戏画面的年龄、每级队列深度与最老任务年龄、OCR 吞吐与丢帧原因、CPU/内存/GC/磁盘写入、漏节点/重复节点/字段 Unknown 比例。

**E4 一次只允许一个写入者**：编码/测试/审计职责分离；我自己保持串行单写入者，同一时刻只改一个子系统；需要对比时用独立 worktree/目录，绝不并行修改同一份源码。

**E5 固定顺序 10 步门禁（每次提交）**：①修改前红测 → ②单一根因补丁 → ③聚焦测试 → ④全量测试 → ⑤整局功能回放 → ⑥原速性能回放 → ⑦候选与基线差异报告 → ⑧人工检查实际 diff → ⑨短局影子运行 → ⑩用户验收。任一步关键字段/性能退化，立即撤销该提交，禁止在坏提交上叠第二层补丁。

**E6 影子运行（Scientist 方法）**：候选版与基线同读同一局画面——基线继续负责用户可见结果，候选写入独立临时目录（不影响真实历史），自动对比节点/字段/错误/延迟/资源消耗，差异清零后才切换给用户使用。

### 阶段 0：冻结基线（0 代码改动）
- 工作副本首次构建 + 全量测试，记录 23 项失败精确清单与运行环境（dotnet --version 等）。
- 产出：`BASELINE-0.2.855.trx` 存档，作为后续"无新增回归"的比较基准。
- 执行 E1：Git init + baseline 标签（基线版本待用户拍板：推荐 0.2.850-bond-bonus-reviewed，备选 0.2.842）。

### 阶段 1：修封存状态机（第一优先，用户可见 bug）
1.1 修条件①：`IsSettlementSummaryReady` 移除 `SettlementGoldReward.Known` 要求（字段已废弃，保留伤害两条件 + 可选金币已知加分）。
1.2 新增第五 exit 条件"确认节点推进封存"（核心修复）：
   - 当 tracker 持有节点 N 的充足备战证据（最后稳定备战）且观察到 N+1 的确认备战/结算（`RunResumePolicy` 后继关系），**且 N 是奖励关（1-1/1-2/1-8/2-6/3-6）或 N 无任何战斗证据**时：用 N 最后稳定备战 + FinalizeBattle 的奖励关规则（或"无战斗伤害=未记录"降级证据）封存 N，幂等、不伪造伤害、不补跳过的中间节点。
   - 与产品合同对齐：奖励节点不因无战斗页永久丢失；缺证据显示"未记录"。
1.3 红测 A（先写测试后写实现）：用 run-20260814-143614 中 1-1 的真实帧序列（按捕获时间排序）回放，断言 1-1 被封存、无战斗伤害、checkpoint/历史一致。
1.4 回归门禁：现有 23 项不增加；1-2/1-3/1-4 封存行为不回退（用同 run 回放断言）。

### 阶段 2：建真实回放基线（P0 补课，性能与封存共用）
- 将 run-20260814-143614 的 41 张截图 + 21 个 analysis 时间线固化为内置 fixture 全管线回放（进截图→分类→识别→tracker→封存→checkpoint→投影→历史 UI 每一层，不允许绕过构造最终对象）。
- 把现有可选门控 `Phase2CapturedDatasetReplayTests`（环境变量）改造为内置 fixture 版本。
- 产出指标：捕获→落盘延迟、analysis 延迟、最终 UI 节点列表与 checkpoint 一致性断言。

### 阶段 3：性能修复（三语义通道架构 + 按风险从小到大，每步有数据）
- **架构原则（吸收前任总结）**：实时识别拆三种语义通道：
  1. **当前状态通道**：只保留最新帧，旧普通帧直接覆盖（有界队列+明确丢弃策略，不处理每一张旧帧）；
  2. **关键事件通道**：战前最后阵容、节点切换、奖励页、结算帧——必须保护，禁止被普通丢帧策略删除；
  3. **持久化通道**：只保存融合完成的状态和必要证据，截图/诊断文件异步落盘，禁止阻塞实时识别。
- 3.1 低风险先做：①出队即判过期（同节点同页面已处理帧跳过，不等识别完）；②收集循环写盘异步化（PNG/JSON 移出关键路径，checkpoint 保持节流）；③投影 Observe 节流（状态未变不重建）；④重复 OCR 消除（situation 快路径与 operational 同区域复用）。
- 3.2 中风险：⑤OCR 真取消/隔离（PP-OCR 超时后释放 session 并限并发，或移到独立进程/长任务组）；⑥识别 worker 数按机器核数受控（默认 1，不超过 2）+ 关键帧优先通道。
- 3.3 每步验收（E3 指标）：按真实时间持续喂帧的回放下，capture→UI P50/P95/P99、capture→finalized、UI 年龄、队列深度、OCR 吞吐/丢帧原因、CPU/内存/GC/磁盘、漏节点/Unknown 比例，同序列对比 0.2.850。目标：capture→落盘 P95 分钟级→秒级。
- 3.4 红线：性能优化以跨帧变化检测为核心，不得退化成逐帧全量 OCR；不得牺牲识别正确率、节点完整性、封存可靠性（用户合同）；不得丢失关键事件帧。

### 阶段 4：23 项失败清零（分组处理）
- A 组 10 项：向用户确认语义后统一改（首选：测试显式传 `confirmationFrames:2` 保持防抖语义测试 + 生产 1 帧不变；或改断言为 1 帧契约。**需用户拍板**，因涉及"识别到即用 vs 连续 2 帧确认"的产品行为）。
- B 组 5 项：从用户 8-11 交接包补 tools/docs 文件。
- C 组 8 项：实跑逐项定责，修代码或修 fixture 契约（不允许为过测试降识别阈值——用户红线）。

### 阶段 5：字段识别逐项修复（P3，每项真实夹具）
金币跳节点保留 → 敌人阵营持久化 → 投资策略/负面词条 → 星级稳定融合 → 7 件背包+特殊物品（垃圾袋/冶金炉/扳手）→ 战斗总伤害 → 奖励关新角色（无可靠 UI 区域前保持 Unknown）。

### 阶段 6：显示与发布（P4）
- 移除专家顾问栏及读取链（用户已要求，旧 JSON 字段兼容读取）。
- 特殊物品图片显示已做，其余显示不动。
- 发布：全新目录、洁净发布（无 WebView2/logs/runs/用户设置）、EXE/DLL/ZIP 哈希一致校验、独立 smoke 目录启动冒烟。

## 3. 每个阶段的通用门禁（用户合同）
1. 改动完成必须 review（含"是否把原本修好的功能改坏"回归检查）。
2. 全量测试不新增失败（相对阶段 0 基线）。
3. 交付目录 self-contained + 完整路径 + 需实机验证点清单。
4. 发布前必须通过 NARRATIVE 第 10 节 9 项最低验收门槛（含"从 1-6 开始不伪造 1-1/1-2""1-4 跳 1-6 不伪造 1-5""连续多节点+奖励关整局""退出后节点完整"）。
5. 禁止：向用户保证"肯定可用"；编译/单测成功当交付依据；只测单张图宣称性能提高；丢关键帧；未知字段自动补全；在交付目录做启动测试。

## 4. 下一步动作（等用户确认后执行）
1. 阶段 0：工作副本构建 + 全量测试基线（约 1-2 小时，低并发低优先级跑，防卡用户电脑）。
2. 阶段 1.3：先写红测 A（用真实 run 回放 1-1 序列）。
3. 阶段 1.1/1.2：实现封存修复。
4. 阶段 1.4：回归 + review + 交付候选版实机验证。
