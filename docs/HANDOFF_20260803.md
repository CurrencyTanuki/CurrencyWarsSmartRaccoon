# 交接总结（2026-08-03 更新，Reasonix → 新会话）

> 本文件为**最新**交接入口。原 0.2.789 交接内容保留在下文历史区。
> 当前版本：**0.2.800**（0.2.790 基线方案重写版，2026-08-03 发布：
> `artifacts\CurrencyWarsSmartRaccoon-0.2.800-win-x64-portable\CurrencyWarsAssistant.App.exe`）
>
> **0.2.800 核心**：以 0.2.790 为开发基线（0.2.791~0.2.798 视为不存在）
> 按用户方案重写——①0-1 延时取消；②标准博弈盲点连点（0.05s×4s→敌人概览
> →确认→0.05s×3s→投资环境点选+复点+50ms 直接确认）；③备战页立即识别/
> 重试200ms/拖拽650+50/去占用复核/页面确认单帧化；④设置页快速刷开局 3 模式
> （稳定/快速/极速，默认稳定）；⑤自动战斗检测重写（0.2s 快速×2s + 2s 慢速）；
> ⑥1-2 复用 1-1 逻辑；⑦开晶矿逻辑确认保留。全量 647/647。
> 详见 `docs\UPDATE_NOTES_0.2.800.md`。
> 0.2.796；③盲点触发条件改为标准博弈即启用（不再要求选快速版）；④盲点
> 间隔 0.05s→0.1s。全量 647/647。详见 `docs\UPDATE_NOTES_0.2.796.md`。

## 项目位置
- 项目根：`C:\Users\zzz81\AppData\Roaming\reasonix\global-workspace\CodexHandoff-20260802\CurrencyWarsSmartRaccoon-CodexHandoff-20260801\`
- 语言：.NET 8 WPF；崩铁"货币战争"玩法辅助工具（识别/记录/刷开局/攻略建议）

## 关键约定（详见 memory：currency-wars-batch-fix-workflow / handoff / deliverable-directory）
- 交付物 = publish 可运行目录（不打 zip）；交付给 exe 完整路径
- 修复流程：累积制（反馈入 `docs/PENDING_USER_FIXES.md`）→ 单变量 → 真实帧回放测试 → 全量回归 → 交付
- 杀旧实例：`schtasks /run /tn "CWTKillApp"`（计划任务提权，无需弹 UAC）；启动新实例会弹 UAC（requireAdministrator）
- 截图看不了就调 `tools\describe_image.py`（千问视觉中转）
- 用户已关闭 Windows 保护（UAC 弹窗仍要点一次）

## 0.2.791（节点反复识别修复——用户 8/3 实测投诉后）

### 修复内容（保守，只改判定与文案，未动识别输出）
1. `NodePattern` 正则 node 范围 `[0-9]`→`[1-9]`：拒绝 1-0 非法节点号（Phase2OperationalScreenshotAnalyzer.cs）
2. `ReadLocalizedNodeDigits` stage 识别 0~9→1~9（同源修复）
3. `Phase2OperationalStateTracker.PageTransitionMessage` 改用 `effectivePage`：节点未知的战斗帧不再输出假"战斗开始"
4. 曾试"节点连续 2 帧才采信"→ 回退（破坏 1-1→1-2 合法推进，多测试失败）
- 详见 `docs\UPDATE_NOTES_0.2.791.md`
- 新增测试 `Phase2BattleNodeStabilityTests`（3 个）全过；相关 187 个 tracker 测试全过
- 全量 653 中 4 个资源敏感测试全量并行时偶发失败，单独跑全过（本机 qianwen 进程占 CPU 34%）
- 交付：`artifacts\CurrencyWarsSmartRaccoon-0.2.791-win-x64-portable\`（exe 0.2.791.0）

### ⚠️ 待办（用户确认后处理）
- **银河学者设置**：user-settings.json 中 `EnableGalaxyScholarRewardStrategy=False`（用户确认未手动关闭，疑似默认值问题）——代码与传递链完整，未改设置逻辑（避免引入新变量）；下一步确认设置加载默认值与"默认出售场上角色"被关的问题
- 备战识别性能：实机 0.2.790 中备战识别 3.2s（0.2.789 时 970ms）——本机 qianwen 进程占 CPU 疑似主因，待用户实机复测确认

---

## 0.2.790（本轮，8/3 刷开局优化——用户指定方案）

### 1. 快速点击路径（方案第 1 项）✅
- 新增 `CurrencyWarsNavigationOptions.FastPathFromHome`：Standard 模式由 `OpeningRerollLoopCoordinator` 传入 true（超频保留原稳定识别路径）。
- 从 `currency_wars_home` 起，固定按钮连点：`start_currency_wars` → `select_standard_mode` → `enter_selected_mode` → `start_run`，每步点击后只做**单帧确认**页面切换（`FastWaitForPageAsync`，poll 150ms），不再等待 2 帧稳定识别。
- **敌人概览保留稳定识别**（`WaitForStablePageWithRecoveryAsync`）——必须先确认识别敌人信息再点确认。
- 位面进度页"一出就点"：`enemy_overview_next` 点击后等 `plane_progress` 用单帧确认（`FastPathUsesSingleFrameConfirmation`）。
- 实现：`CurrencyWarsNavigation.cs` 新增 `FastPathFromHomeAsync` / `FastWaitForPageAsync` / `FastPathUsesSingleFrameConfirmation`；常量 `FastPathStepDelay=300ms`、`FastPathPollInterval=150ms`、`FastPathPageSwitchTimeout=8s`。

### 2. 投资环境识别提速（方案第 2 项）✅
- `continue_plane_progress` 点击后等待投资环境页也用单帧确认（页面一出现即开始内容识别）；识别可靠性由 `ReadStableInvestmentEnvironmentsAsync` 自身 2 帧投票稳定兜底，**识别结果不变**。

### 3. 备战页大头优化（方案第 3 项）✅（只改等待时长，不改识别逻辑与输出）
- 只读子代理分析定位全部可安全缩短的保守等待（均有验证循环兜底）：
  - `InitialPreparationSettleDelay` 1400→**800ms**
  - 商店补员后回备战页 900→**600ms**
  - 银河学者替换后重识别前 500→**350ms**
  - 部署/出售/Esc 后 `AfterActionDelay` 500→**350ms**（×3 处：行 1481/1629/1944）
  - 页面确认 2 帧间 200→**150ms**（CaptureVerifiedPreparationAsync）
  - 金币/占用复核帧间 300→**200ms**（×2 处：行 1402/1711）
- **未动**（与正确性相关）：550ms 备战席动画稳定重试、650ms 拖拽时长、连续 2 帧确认语义（页面门禁/拖动验证/出售验证）、3 次/视觉识别上限。
- 优化后 1-1 乐观路径约省 **2.9-3.9s/轮**。

### 测试与交付
- 新增 `CurrencyWarsNavigationFastPathTests.cs`（3 个集成测试，真实 NavigationTask + fake 依赖页面序列回放）：
  - 快速路径点击序列 + 敌人/投资环境识别保留
  - 非快速路径同序列回归
  - 敌人概览未出现时安全停止、绝不在确认前点确认
- 全量回归：**650/650 通过**（647 + 3 新增）。
- 交付目录：`artifacts\CurrencyWarsSmartRaccoon-0.2.790-win-x64-portable\`，exe 版本 0.2.790.0。

## ⚠️ 需实机验证点（0.2.790）
1. **快速点击路径**：主界面→开始本局是否明显变快；标准模式连点是否稳定到达敌人概览（动画长，等待超时 20s 未变）。
2. **位面进度一出就点**：确认后是否立即继续动画，不再等页面稳定。
3. **投资环境页单帧确认**：页面出现即识别，识别结果是否仍完整（3 张卡）。
4. **备战页提速**：1-1 部署/出售是否正常完成（拖拽 650ms 未动，主要省的是保守等待），识别结果与 0.2.789 一致。
5. **CWTLaunchApp 计划任务仍指向 0.2.780**（更新需管理员权限，本会话未改）——如用户用桌面 vbs 启动，请先手动更新计划任务或直接双击 0.2.790 exe。

## 待办（来自 0.2.789 交接，未变）
1. 金币"70几→7"：若全候选都识别成 1 位（区域截断），需 2-5 备战截图再确认区域宽度
2. 入场过场误判战斗（fast 分类器误判 + 节点号 OCR 误读 1-9 叠加；completionNodeId 已绕开，悬浮窗中间过程可能短暂出现；待 fast 层修复）
3. 退出真实测试（verify-exit.ps1 已写好未跑，用 powershell 跑）
4. PENDING_USER_FIXES.md 中未修复项（日志/历史窗位置、1-3 血量Δ、1-4 卡死 dump 定位、纯补给跳过节点、结算快速点击补齐等）

## 技术要点（新会话可能用到）
- 杀进程：`schtasks /run /tn "CWTKillApp"`；启动：Start-Process exe（UAC）或更新 CWTLaunchApp 计划任务后 `schtasks /run /tn "CWTLaunchApp"`
- 构建：`dotnet build CurrencyWarsAssistant.sln -c Release`；测试：`dotnet test -c Release --no-build`
- 发布：`dotnet publish src\CurrencyWarsAssistant.App -c Release -r win-x64 --self-contained -o artifacts\...`
- 版本号：`Directory.Build.props` 的 `<Version>`
- 会话日志：`%LOCALAPPDATA%\CurrencyWarsSmartRaccoon\logs\test-session-*.jsonl`（JSONL，Message 字段）
- 存档：`%LOCALAPPDATA%\CurrencyWarsSmartRaccoon\runs\run-*/`（completed-run-v1.json + analysis-*.json + screenshots/）
- 报告脚本：`report-demo\gen_report.py`（按实际节点生成 HTML，无模板）；已复制进 App 发布目录
- 测试图片已转桌面：`C:\Users\zzz81\Desktop\货币战争测试图片-20260803\`（98 张）

## 刷开局流程参考（用户梳理，2026-08-03）
主界面→指南(右上)→第3页签→前往参与货币战争→关弹窗(Esc)→开始货币战争(1590,976)→模式选择→进入所选模式→开始本局→敌人概览(识别3阵营+4词缀)→位面进度(点空白)→投资环境(识别3卡)→备战1-1(出战1786,752)→投资策略(选/放弃)→保存退出循环。
- 固定（不需识别）：所有按钮位置、槽位布局（敌人3+4、投资环境3卡、备战4+6+4）
- 随机（必须识别）：投资环境3卡（83池）、敌人阵营/词缀（51池）、投资策略（334池）

---

## 历史：0.2.789 交接（原文档，保留）
- 0.2.779-0.2.789 共 11 个版本：历史 UI 字段清理+报告窗口、关键帧队列丢最旧保新+过场动画丢弃+相似帧去重、识别节律 2s+空转修复+战斗开始去抖、节点号三连修、金币前缀匹配、刷开局延迟全面下调+投资策略三连刷。全量 647/647。
- 进行中（已完成于 0.2.790）：快速点击路径、投资环境识别提速、备战页优化。
